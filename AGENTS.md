# Codex 项目执行规范

## 角色分工

- Codex 默认直接负责架构设计、安全审计、跨模块推理、只读盘点、文档整理、具体实现、Bug 修复和验证。
- 不要求把任何任务交给指定外部代理；仅在用户明确要求时启用代理协作。
- Codex 必须复核最终结果，并把高频问题沉淀回本文档。

## 当前系统现状与目标技术栈

- Phase 0–6 的规范、应用代码、迁移工具、生产编排、发布证据门禁和标准 MCP
  服务已经交付；真实联调、生产等价演练、UAT、切换与 Hypercare 尚待现场执行。
- 强制规范入口：`docs/phase-0/README.md`；当前状态摘要：`CODEX.md`；Codex
  执行入口：`.codex/AGENTS.md`。
- 当前形态：NestJS 模块化单体 + Next.js App Router + MongoDB Replica Set +
  Redis/BullMQ + Helm/Kubernetes。
- ERP 是组织与员工唯一主数据源；多租户从可信身份上下文强制；MCP 随业务模块
  同步交付。

## 通用编码要求

- 回复、代码注释、文档注释、迁移说明、Git commit message 必须使用中文。
- 禁止硬编码密码、密钥、token、银行账号敏感信息和电子签密钥。
- 危险操作必须先问用户，包括但不限于 `rm -rf`、`git reset --hard`、`git push --force`、删库、批量覆盖配置。

## SQL / Mongo 查询与安全红线

- 动态排序、动态字段、自定义字段查询必须走白名单映射。
- SQL 注入、Mongo 查询注入、XSS、权限绕过数据泄漏必须打回。

## 执行标准

- Codex 默认直接执行任务，不以外部代理参与作为前置条件。
- 只读任务必须遵守禁止修改文件、删除、格式化和提交的边界。
- 大范围盘点要拆成小批次，避免全仓探索阻塞主线。
- 输出必须包含必要的文件路径、调用方向、迁移建议或实现清单。
- 所有实现结果必须经过复核和验证，不能未经检查直接合并。

## 验证命令

- 默认执行 `pnpm install --frozen-lockfile`、`pnpm audit`、`pnpm check` 和
  `pnpm build`。
- 目标环境、外部系统或生产权限缺失时，必须明确区分“本地验证通过”和
  “外部验收未完成”，禁止用自测替代现场证据。
- 如果本机缺少对应构建工具，必须在最终回复说明“未能验证”的原因。

## 工作标准（Codex CR 沉淀区）

### 来自 CR #1

- [ ] 临时调试日志、重复索引警告、Mongoose warning 不能带着进入提交。
- [ ] 静态测试要避免全文件误判，校验具体方法片段或关键编译产物。
- [ ] 推送类动作必须校验源对象处于可推送状态，不能用“未归档”等宽泛条件替代业务状态。

### 来自 CR #2

- [ ] 禁止信任客户端传入的租户标识，租户上下文必须来自已验证身份。
- [ ] 敏感字段加密检索必须使用盲索引，禁止直接索引明文或随机密文。
- [ ] MCP 业务能力必须复用应用服务；禁止直接访问数据库、透传上游Token或让AI执行R3操作。
- [ ] 每个业务模块必须同步交付REST、事件、MCP契约和审计点，不得后期集中回填MCP。

### 来自 CR #3A

- [ ] 文档中的 REST 路由、签名串、字段名、事件 type、MCP Tool/Resource/Prompt 必须与实现逐字一致，并由协议测试覆盖。
- [ ] 阶段文档必须区分“代码已交付”“外部联调待验收”和“后续切片”，禁止把未实现的区间查询、事件或运维控制面写成已交付能力。

### 来自 CR #3B

- [ ] 加密字段的 schema 必须按真实编码后长度校验 IV、AuthTag、摘要和密文上限，组合字段约束不得只依赖单字段正则。
- [ ] 扩大共享 Secret Resolver 的引用范围时，各业务消费者仍须二次校验自身命名空间，禁止跨用途读取密钥。
- [ ] 外部副作用或数据库事务提交后的审计异常必须单独分类，禁止通用失败处理把已成功终态回写为失败或再次执行外部创建。
- [ ] Worker 模块只装配后台任务所需仓储与服务，禁止为复用少量 Provider 引入完整 HTTP/OAuth 模块依赖图。

### 来自 CR #4

- [ ] 只读任务即使进入内部 Plan 模式也禁止写计划文件；一旦准备写入必须中止，不能以内部流程覆盖任务边界。
- [ ] 审计成功写入失败不得被通用 catch 追加为“业务失败”，必须区分业务执行失败与业务已提交后的审计故障。
- [ ] HMAC 审计链只能声明“可检测篡改”；未完成独立权限域 WORM 锚定前，禁止宣称完整不可抵赖。

### 来自 CR #5

- [ ] Schema 字段声明 `default: null` 时，自定义校验器必须显式接受 `null`，并覆盖启用与未启用两种配置状态。
- [ ] 领域状态、进度和证据组合不变量必须同步下沉到持久化校验边界，并用完整状态矩阵防止受损记录绕过应用服务。

### 来自 CR #6

- [ ] R3 运维 CLI 必须拒绝重复、未知或非规范化参数；数值版本只接受明确的十进制正整数，禁止宽松类型转换。
- [ ] 直接读取数据库执行修复或重放前，必须重新验证权威记录的引用、状态与证据组合；不得把受损字段强制转换成看似合法的缺省值。
- [ ] CLI 成功与失败路径都必须关闭外部连接，终端错误只暴露稳定错误码，不得输出连接串、凭据或数据库异常正文。

### 来自 CR #7

- [ ] MCP 确认执行必须在完成一次性确认账本前校验、脱敏并序列化返回值，尤其禁止把非法 Resource Link 记为已完成。
- [ ] MCP 业务与确认账本均已提交后，审计故障只能形成稳定告警；禁止释放已完成操作、重复业务副作用或向 AI 反向暴露失败。

### 来自 CR #8

- [ ] 外部系统入站 Worker 必须先持久化业务或 Inbox 终态，再写提交后审计；审计故障不得反向改写终态、覆盖原始异常或触发业务副作用重试。
- [ ] 外部连接器返回的错误码进入数据库、审计或指标前必须通过稳定格式校验；非规范或自由文本错误统一映射为本域固定错误码。

### 来自 CR #9

- [ ] Outbox Relay 必须逐字绑定信封顶层、数据身份与 Outbox 元数据；只校验部分身份字段不得作为可信外发依据。
- [ ] 跨系统终态桥接必须用读取到的精确旧状态与旧版本做乐观锁，禁止先生成投递后用宽松版本条件静默推进；已有投递必须复核全部不可变控制字段。
- [ ] 外部副作用成功或失败终态落库后的审计故障不得中断批次、掩盖原始终态或触发相同副作用重试，只允许记录不含外部正文的稳定告警。

### 来自 CR #10

- [ ] 全仓覆盖率必须显式 `include` 全部生产源码并在 `pnpm check` 中执行全量覆盖命令；只统计被测试加载的文件不得称为“全量覆盖率”。
- [ ] 全局 80% 与关键链路逐文件 90% 必须同时失败关闭，禁止用若干高覆盖专项报告替代全源码分母。
- [ ] Webhook 验证拒绝或成功入箱后的审计故障不得覆盖原始 HTTP 决策；只能记录不含签名、nonce 和正文的稳定告警。

### 来自 CR #11

- [ ] 公共协议端点完成独立 Cookie、签名或客户端认证后，必须使用显式可信用户/服务审计接口；禁止调用依赖 Bearer 请求租户上下文的通用审计接口。
- [ ] R1/R2 确认状态推进后的审计故障不得撤销确认、重复消费强认证仪式或向客户端反向暴露失败，只允许记录不含命令、凭据或断言正文的稳定告警。
- [ ] MCP Origin Guard 必须在身份认证前覆盖框架可路由的规范路径与尾斜杠路径，控制器继续执行二次校验；路径别名不得形成 DNS rebinding 绕过。

### 来自 CR #12

- [ ] 跨系统全量快照的摘要与游标必须绑定可信租户和契约版本；游标只接受规范 Base64URL、精确字段集和服务端生成的合法页边界，主数据变化后旧游标必须失败关闭。
- [ ] 批量主数据读取只允许可信服务或系统任务身份，并记录不含人员正文的计数/摘要审计；读取审计不可用时必须失败关闭。
- [ ] 面向专业系统的批量同步接口保持专用服务身份 REST 边界，禁止为方便 AI 对接而注册为 MCP Resource 或批量导出 Tool。

### 来自 CR #13

- [ ] 资金、薪酬、考勤等写控制器在业务事务、WORM 或外部副作用提交后必须隔离审计故障；禁止向客户端反向暴露失败并诱发重复追加、重复申报或重复提交。
- [ ] 提交后审计告警只记录稳定错误码、动作、资源类型、资源标识和风险级别，禁止记录审计异常正文、工资/账户/考勤载荷或访问令牌。
- [ ] 敏感读取与写入终态采用不同审计语义：读取审计不可用时继续失败关闭；写入已提交后只告警，不得用统一 catch 混淆两者。

### 来自 CR #14

- [ ] 身份租户绑定、外部身份映射和授权快照属于信任边界；数据库最小投影必须经过
  运行时租户、平台、状态、双标识、集合唯一性与版本校验后转换为深冻结普通对象，
  禁止把活动 ODM 文档直接交给令牌签发或授权流程。
- [ ] 人员 SSO 必须逐层绑定一次性 state、适配器注册槽、外部平台 tenantId、ERP
  tenantId 和精确身份映射；任一漂移、映射缺失或持久化记录受损均失败关闭。
- [ ] SSO 上游 Token 只能存在于固定端点适配器内部；禁止重定向、任意 URL/query、
  非 HTTPS/443 和无界响应，且不得向 REST、MCP、仓储、日志或业务服务透传。
- [ ] 浏览器一次性 state Cookie 必须拒绝超长 Header、重复同名值和非规范摘要，
  所有回调终态（包括 Origin/provider 拒绝）都要清除；构造 Redis Key 前必须验证
  state 规范长度与字符集，基础设施故障不得降级或伪装为普通状态失效。

### 来自 CR #15

- [ ] 审批主体解析不能只信任 active 授权快照；必须同时复核 ERP 员工仍为
  active/probation，并精确绑定 tenant、actor、employee 与部门范围。
- [ ] `department_manager` 解析器只能引用模板中的 department 类型字段，禁止
  以文本或其他字符串字段变相选择审批部门。
- [ ] 角色、员工和部门仓储返回值必须在应用边界复核租户、查询标识、状态与唯一
  映射；持久化漂移失败关闭，单节点解析人数不得超过 100。

### 来自 CR #16

- [ ] 存活探针不得访问依赖；就绪探针必须有界验证 MongoDB 可写 Replica Set
  主节点和 Redis PONG，并对并发重连执行单飞，禁止探针制造连接风暴。
- [ ] Prometheus 标签中的客户端可控 HTTP Method/状态必须收敛到固定集合；
  未知异常和 5xx 不得向 REST、MCP 或探针响应泄漏正文、堆栈或连接信息。
- [ ] Redis URL 转换器必须独立复核协议、主机、规范数据库路径、端口和凭据编码，
  明确支持 `rediss://`，禁止静默忽略 query、fragment 或额外路径。

### 来自 CR #17

- [ ] 外部系统专用 HTTP 客户端必须固定 HTTPS origin、方法、路径和协议 Header
  白名单；禁止调用方覆盖 Host、Accept、传输编码或注入协议外 Header。
- [ ] 签名出站正文必须在外呼前验证为有界 JSON 对象；响应即使省略或伪造
  Content-Length，也必须流式限长并使用严格 UTF-8 解码，成功正文须为 JSON
  Content-Type。
- [ ] 非 2xx 分类不得依赖或泄漏上游错误正文；网络、读取、解析与取消异常只进入
  本域稳定错误码，禁止把 cause、签名、凭据、请求正文或上游响应交给日志、审计、
  REST 或 MCP。

### 来自 CR #18

- [ ] 银行、税务等 R3/R4 外部副作用适配器必须二次校验端点、凭据、运行模式和
  短时生产授权；配置层校验不得替代运行时失败关闭。
- [ ] 资金出站回执必须同时执行声明长度与实际流式长度上限，严格校验 UTF-8、
  JSON Content-Type、完整 Schema 和原批次控制量；读取器取消或释放异常不得覆盖
  已确定结果。
- [ ] 非 2xx 银行响应禁止读取正文；生产回执必须精确回显授权、发布 commit 和
  部署清单摘要。REST、事件和 MCP 均不得暴露银行凭据或获得直接资金执行能力。

### 来自 CR #19

- [ ] 资金终态入站适配器必须固定标准 HTTPS 方法、路径、协议 Header、独立凭据
  和确定性幂等键；业务调用方、REST、事件与 MCP 均不得覆盖传输契约。
- [ ] 外部回盘成功清单必须同时执行规范 Content-Length 与实际流式长度上限，
  严格解码 UTF-8/JSON 并完整校验 Schema 和租户、批次、上游回执绑定；非 2xx
  禁止读取正文，读取、取消或释放异常只允许返回本域稳定错误码。
- [ ] 验签失败、恶意文件或部分失败等负面证据不得被适配器丢弃或美化；必须交由
  资金应用服务按状态机整批冻结。MCP 不得领取、上传或处理银行回盘正文。

### 来自 CR #20

- [ ] L4 税务正文归档与税局提交必须使用相互隔离的固定 HTTPS 方法、路径、凭据
  和幂等域；归档正文必须在外呼前严格解码并绑定可信租户、申报 ULID、对象键与
  内容摘要，禁止只检查字符串前缀。
- [ ] 税务 WORM 与提交回执必须先按非 2xx 状态失败关闭且不读取正文，再执行规范
  Content-Type、Content-Length、实际流式长度、严格 UTF-8/JSON、完整 Schema
  和请求事实反向绑定；读取、取消和释放异常不得泄漏 cause 或覆盖稳定结果。
- [ ] production 税务提交必须二次校验短时独立授权、发布 commit 与部署清单摘要，
  提交标识和证据标识不得混用。MCP 只允许复用应用服务读取脱敏控制摘要，禁止向
  AI 暴露税务正文、WORM 地址、凭据、归档、制备、审批或提交能力。

### 来自 CR #21

- [ ] L4 资金文件写入 WORM 前必须严格验证可信租户、批次 ULID、固定
  pain.001 Schema、唯一 MsgId/PmtInfId、对象键与内容摘要；禁止仅检查 XML 声明
  或允许 DOCTYPE/ENTITY。
- [ ] 资金 WORM 适配器必须固定标准 HTTPS `POST /v1/objects`、独立运行时凭据、
  十年至一百年保留期和确定性幂等域；URL 凭据、query、fragment、重定向、非
  443 端口及其他路径全部失败关闭。
- [ ] WORM 非 2xx 回执不得读取正文；成功回执必须执行严格 JSON Content-Type、
  规范 Content-Length、16 KiB 流式硬上限、Fatal UTF-8、完整 Schema，以及
  租户、批次、对象键、摘要和保留期反向绑定。MCP 不得归档、读取或返回资金文件
  与 WORM 地址。

### 来自 CR #22

- [ ] 隐私删除、匿名化或密钥销毁等外部清理任务在出站前必须重算任务 ID 与控制
  摘要，并复核可信租户、授权版本、目的、终止事实、目标政策、执行租约和尝试上限；
  禁止把受损持久化任务或队列载荷直接送往下游。
- [ ] 清理目标必须使用互不复用的标准 HTTPS 根 Origin、Bearer 凭据、Ed25519
  Key ID 与公钥；运行时仍须二次校验固定方法、路径、协议 Header 和目标登记，
  配置启动校验不得代替出站失败关闭。
- [ ] 下游清理证明只接受 200、严格 JSON、无压缩、规范 Content-Length 与
  16 KiB 流式硬上限；原始字节须先经独立 Ed25519 信任根验签，再执行 Fatal
  UTF-8、完整 Schema、请求上下文、存储类型/引用和最低保留期绑定。MCP 只能读取
  脱敏状态计数，不得执行清理、重放、重建、恢复授权或读取证明。

### 来自 CR #23

- [ ] 私密开户 Worker 不得信任 Mongo 认领记录的静态类型；外呼前必须二次校验
  可信租户、请求 ULID、员工、固定渠道、幂等键、密钥标识、尝试次数和敏感有效期。
- [ ] 平台首次开户回读必须精确绑定 ERP 派生的确定性 userId，并复核外部租户、
  userId 与 unionId；任何漂移都不得建立 AccessProfile、ExternalIdentity 或考勤映射。
- [ ] Mongo 事务成功后的会话清理或审计故障、失败终态提交后的审计故障必须单独
  分类；通用失败处理不得重复平台副作用、回写成功为失败或二次更新已提交失败终态。
- [ ] R3 联系方式不得进入 Outbox、日志、审计 metadata 或 MCP 参数；标准 MCP
  永久不注册开户、重试、凭据、身份绑定或平台写能力。

### 来自 CR #24

- [ ] 外部日历、通知等副作用的过期 `processing` 只表示结果不确定，禁止仅凭
  锁超时自动重放；必须隔离人工核验并保留稳定错误码。
- [ ] 多步平台写入一旦可能部分提交，必须携带经验证的已知外部对象标识进入
  `manual_review`；通用失败处理不得重复创建、覆盖成功或丢失部分提交事实。
- [ ] 延迟不可变事件必须与当前权威投影版本核对；新投影取代旧事件时只能
  `superseded`，禁止使用旧幂等键写入当前业务事实。
- [ ] 结果不确定任务只有完成平台核验后的 R2 `approved_exception` 可恢复或确认
  成功；标准 MCP 永久不注册日历写入、取消、重试、处置、对账、外部标识或凭据。

### 来自 CR #25

- [ ] 多领域迁移入口的 HTTP Scope 只能声明通用执行权；目标域写权限必须由应用
  服务按运行 Scope 动态校验，禁止为所有迁移强绑组织或任一单域写权限。
- [ ] 迁移队列载荷与 JobId 必须互相绑定并由 Worker 重算；持久化任务、运行记录、
  附件记录和终态更新必须在运行时复核可信租户、标识、尝试次数及原租约栅栏。
- [ ] 隔离附件网关回执必须严格回显租户、运行、来源系统和来源附件，并绑定摘要、
  分级与保留期；非 2xx 不读取正文，成功响应执行无压缩、Content-Length、流式
  上限、Fatal UTF-8、严格 Schema 和请求事实反向绑定。
- [ ] 迁移状态或附件失败/成功终态提交后的审计故障只允许独立告警；禁止恢复为
  `pending`、诱导客户端重放或覆盖其他 Worker 状态。MCP 继续只读聚合报告，不得
  暴露写迁移、附件传输、详细证据或凭据能力。

### 来自 CR #26

- [ ] 跨域只读投影不能只信任各仓储查询条件；每个来源服务必须复核返回记录的
  可信租户、查询主键与完整引用闭包，任一错位整体失败关闭。
- [ ] Care、校友等下游事实必须绑定上游已授权的劳动关系、员工和自然人，禁止
  仅凭调用方传入标识拼接案件或授权。
- [ ] 标准 MCP 只能复用已经完成跨域完整性校验的应用服务最小投影；不得直接调用
  来源仓储、绕过数据范围或为便利 AI 新增人才生命周期写能力。

### 来自 CR #27

- [ ] 跨域写桥必须在副作用前复核可信租户、查询主键和完整引用闭包；仓储查询
  条件、静态类型或上游状态声明均不能替代消费者侧失败关闭。
- [ ] 业务阶段事件必须记录驱动状态迁移的受信任证据；新建实体或关联结果的标识
  必须作为独立字段保存，禁止用结果 ID 冒充完成证据。
- [ ] 录用终态必须同时满足合同签署证据、入职完成证据和已落库劳动关系引用；
  标准 MCP 不得绕过受信任内部工作流触发录用或劳动关系建档。

### 来自 CR #28

- [ ] 会在内存解析 L4 数据的跨域窄口只允许同租户服务主体或系统任务调用；
  普通用户即使持有同名内部 Scope 也必须拒绝。
- [ ] 关怀等周期编排读取 Employee、Employment、Person 和历史记录时必须闭合
  查询主键、可信租户、唯一开放关系、当前记录与历史投影；损坏引用不得降级为
  “不具备资格”并触发静默取消。
- [ ] 证明引用、登记时间和盲索引必须成套存在，密钥轮换后无法解析时失败关闭；
  明文日期、盲索引及上游实体引用不得进入事件、队列、日志、审计或 MCP。
- [ ] 标准 MCP 只能读取经过跨域来源校验后由业务应用服务生成的本人脱敏汇总，
  禁止直连 L4 来源窄口或新增关怀写入、发送、重放与对账能力。

### 来自 CR #29

- [ ] 外部批量读取适配器必须把返回主体、嵌套主体、事件标识和事实时间窗口逐项
  反向绑定原请求；固定 HTTPS、令牌和响应 Schema 不能替代业务范围闭包。
- [ ] 平台任务、事件或记录标识重复、互相矛盾、无法稳定派生时必须整批失败关闭，
  禁止静默去重、接受请求外主体或让异常事实推进同步游标。
- [ ] EvidenceVerifier 必须校验规范请求 ID、事件来源绑定、拉取时间与事实时间线；
  破坏性证据语义变更必须提升 Normalizer 版本，并在发布前清空旧版本未决 Inbox。
- [ ] 平台 401 只允许失效当前令牌并重试一次；第二次 401、429、5xx 或未知错误
  必须交给上层退避/人工复核。标准 MCP 不得开放 Provider 拉取、Inbox、凭据、
  重放或人工处置能力。
