# 项目工程规范

## 系统现状与目标技术栈

- Phase 0–6 的规范、NestJS 模块化单体、Next.js App Router、MongoDB
  Replica Set 契约、Redis/BullMQ Worker、迁移控制面、Helm/Kubernetes
  编排和标准 MCP 服务代码已经交付。
- 代码交付不等于生产完成。真实外部系统联调、三次全量迁移、性能、安全、
  容灾、业务 UAT、Go/No-Go、统一切换和四周 Hypercare 仍以现场证据为准。
- ERP 是组织与员工唯一主数据源；多租户从可信身份上下文强制；REST、事件、
  MCP 和 Worker 必须复用应用服务。
- 2026-08-06 首发版已使用 `production/initial` 档位在独立 Compose Project
  上线 `aio.gaoq.com`、`recruit.gaoq.com` 与 `www.gaoq.com`；HTTPS、健康检查、
  未授权拒绝、空职位投影和 CMS 访客站已验证。本次没有执行 MongoDB 迁移、
  演示数据写入、删除或重建。2026-08-08 起招聘门户域名切换为 `joinus.gaoq.com`，
  `recruit.gaoq.com` 停用。独立专业算薪、企业 SSO 与外部资金/税务/eSign/WORM
  仍需完成生产接入，已验证证据和回滚边界见 `deploy/standalone/LAUNCH_STATUS.md`。
- 2026-08-08 已把 GaoQ OS 全量应用与独立专业算薪部署到单机生产：两个
  Compose Project 共 10 个目标容器健康，目标端口仅绑定回环，数据库与 Redis
  均未发布主机端口；既有其他项目保持健康。本次没有执行数据库迁移、初始化、
  种子或删除。2026-08-09 GaoQ MongoDB 已切换为生产机独立单节点 Replica Set
  容器 `gaoq-mongo`（`gaoq-rs0`），严格 `/ready` 恢复 200；
  专业算薪公网入口改为 `aio.gaoq.com/payroll` 路径挂载（不再使用独立域名），
  Nginx 更新仍待人工完成。生产拓扑、镜像、
  回滚、安全边界和人工待办见
  `docs/handover/PRODUCTION_HANDOVER_2026-08-08.md`（同时提供 HTML）。
- 2026-07-29 已交付标准 MCP 本地 stdio 入口，与生产 Streamable HTTP 共用
  `McpRuntimeService` 和 `AccessTokenVerifier`。启动要求秘密管理器注入短时
  Token 与 `erp:mcp:server:connect`，先预检、后逐消息复验，撤销/过期立即关闭；
  stdout 仅传输 JSON-RPC。官方 TypeScript Client 已通过真实字节流发现同一套
  50 Tool、4 个静态 Resource、27 个 Resource Template 与 25 个 Prompt；
  各实体客户端仍须按其已完成范围与外部验收边界单独判定。
- 2026-07-29 已把 stdio 进程入口抽成可测试的失败关闭运行器：环境预检先于
  `AppModule` 动态加载，输入结束、`SIGINT`、`SIGTERM`、连接错误和迟到资源
  使用同一幂等清理状态机；测试发现并关闭了应用加载与信号并发时重复
  `close()` 的竞态。28 项专项测试使三个 stdio 生产边界分别达到逐文件四维
  90%，专项门禁已接入 `precheck/check`；稳定码写入失败和资源关闭失败也只
  提升退出状态，不泄漏 Token、环境、路径或堆栈。
- 2026-07-29 已取得首个厂商实体客户端目录证据：Kimi Code CLI 0.28.1 通过
  正式 ACP 客户端层加载同一 `McpRuntimeService`，经 stdio 报告
  `gaoq-erp: connected (stdio, 50 tools)`。可重复探针只执行本地 `/mcp`
  状态命令，不调用模型、不执行业务 Tool、不依赖数据库或外部系统，结果绑定
  当前 `catalogHash`。这只通过 Kimi 的启动与 Tool 目录发现；正式 Token、
  Resource/Prompt、R0/R1/R2、撤销/重连和业务 UAT 未验收，Kimi 整体以及
  Claude、Cursor 仍无实体证据。Inspector 后续已完成四类目录实体发现，但其
  正式 Token、授权业务调用和 UAT 仍保持 No-Go。
- 2026-07-29 已把 `catalogHash` 从 Tool 摘要升级为四类完整能力目录摘要：
  从同一运行时实时解析 50 Tool、4 Resource、27 Resource Template、25 Prompt，
  并绑定忽略注释/格式差异的 `runtimeContractHash`。锁定且隔离的官方 MCP
  Inspector CLI 2.0.0 已实体执行四个 list 方法并逐项匹配目录；探针不读取
  Resource、不渲染 Prompt、不调用业务 Tool/模型/数据库/外部系统。Phase 5
  MCP 联调和总 Go/No-Go 门禁均精确绑定完整摘要及数量，陈旧 30/3/2 示例不再
  可通过。该证据只关闭 Inspector 四类目录兼容缺口；正式 Token、授权读写、
  远程 OAuth、撤销/重连和业务 UAT 未完成，Inspector 整体仍保持 No-Go。
- 2026-07-30 已落实 Phase 0 字段级映射强制项：
  `apps/erp-api/src/modules/integration/adapters/` 分别记录钉钉、飞书、OP、
  e签宝、招聘渠道、银行文件、税务文件和通知服务的 canonical/外部字段、
  权威方向、幂等回执与敏感性边界；`scripts/validate-docs.mjs` 将映射索引、
  九个文档和关键契约标记纳入 `pnpm check`。这只关闭仓库映射文档缺口，不
  代表真实企业租户、银行、税局、e签或通知服务已联调。
- 2026-07-30 已把 Phase 0 “每个业务 Issue 同步列出 REST、事件、MCP”转成
  实时 GitHub 治理门禁：27 个 Story 正文全部按三面契约补齐，未新增的接入面
  也明确安全理由；`GOV-STORY-CONTRACT-SURFACES` 和第 24 个负向自测会拒绝
  任一缺面。实时治理复核 7 个 Milestone、51 个 Issue、79 个 PR 通过，原外部
  验收项均保持开放且未被勾选。
- 2026-08-06 已完成 GitHub 治理收口：逐项证明 66 条历史开放 Draft 的 head
  commit 是 `main` 祖先，并证明 #107/#109 已由 #110/#112 及综合集成 #104
  替代，共关闭 68 条历史 Draft；未删除任何远端分支或代码，外部验收 Issue
  继续保持 OPEN。当前仅保留 12 条依赖维护 Draft。实时治理复核 7 个
  Milestone、51 个 Issue、104 个 PR 通过，远端 run `31084698685` 成功。
  `main@9e2ab5e` 的 run `31082906936`、`31082907668` 和 `31082906778`
  分别通过工程质量、安全与供应链及文档门禁；Project 仍因缺少
  `read:project,project` 最小权限由 Issue #41 阻塞，现场验收边界不变。
- 2026-07-30 已新增 Phase 0 数据处理活动登记册，19 类数据活动逐项登记分级、
  目的、待现场确认的依据、来源、共享、存储、保留/删除和负责人，覆盖外部
  集成、专业算薪、MCP、迁移、审计及发布证据；文档门禁锁定必需列和关键治理
  声明。该仓库总账不替代 PIA、适用法域法务结论、真实接收方核验或数据/法务/
  安全/业务四方逐行签署，未签署时仍为 No-Go。
- 2026-07-30 已建立 `docs/adr/`：六项 accepted ADR 正式冻结模块化单体、
  ERP 主数据/双平台、MCP/AI 风险、专业算薪事实源、GitHub Hosted OIDC 和
  统一切换/证据门禁。每项均记录背景、决策、后果、否决方案、安全/数据影响、
  Issue 与替代关系，并由文档门禁锁定；accepted 不代表外部验收或生产批准，
  改变结论必须新增替代 ADR。
- 2026-07-30 已建立企业威胁模型：8 类关键资产、9 个信任边界和
  TM-001–TM-020 的 STRIDE 登记覆盖租户/身份、事件、证据、L3/L4 泄漏、
  DoS、MCP/提示注入、工资资金与供应链；每项分别列仓库控制和现场残余风险。
  文档门禁锁定核心威胁与 Phase 0 三方签署要求；真实 DAST/ASVS、红队、角色
  矩阵、外部密钥轮换和三方签署仍未完成。
- 2026-07-30 已建立源码确定性生成的 OpenAPI 3.1 REST 基线：47 个
  Controller、225 个 Nest 路由声明、218 个 Path 和 231 个 Operation 均记录
  精确 OAuth Scope、公开状态、Guard、Header/Query/Path/Body、成功响应与源码
  位置。103 个 class-validator DTO 已展开字段级约束；116/116 个 Body 全部绑定
  命名组件（85 个 DTO、27 个运行时 Zod、4 个编译器内联类型），共 135 个
  Component Schema。231/231 个成功响应包括 229 个显式内容 Schema、1 个 302
  和 1 个 204；TypeScript Program 展开跨文件/推断返回值，RawResponse 从实际
  `json/send/redirect` 提取状态码与媒体类型，OAuth 表单、CSV、MCP SSE 和
  WebAuthn 不再误报。生成器拒绝未命名/unknown 写请求、顶层 unknown 成功响应、
  失效运行时来源、动态路由/Scope、重复操作、路径参数漂移、公开/Scope 冲突及
  悬空引用，自测与逐字节漂移校验通过 `pretypecheck` 接入 `pnpm check`；
  2026-08-01 已在 `main` 的 GitHub Hosted Actions 实际通过 Phase 1 工程质量、
  Phase 5 安全与供应链及文档质量门禁；该结果不替代外部系统和目标环境验收。
- 2026-07-30 已建立 AsyncAPI 3.0 / CloudEvents 1.0 机器契约：从固定生产
  Outbox 与专业算薪平台契约确定性收集 184 个现行事件，其中 ERP/平台出站
  180 个、专业算薪入站 4 个；每项声明固定 source、方向、权威系统、分级、
  幂等规则和运行时 Schema 源。生成器排除兼容窗口旧 type，拒绝动态
  `EVENT_TYPES`、重复 type、来源缺失及目录漂移，并与 OpenAPI 一起通过
  `pretypecheck` 接入总门禁。物理 Broker、Topic ACL、mTLS、保留/死信和
  逐 type 外部联调仍是目标环境验收项，不能由仓库目录替代。
- 2026-07-29 已关闭审批 MCP 的 Schema 方言与机器可读风险分级缺口：锁定的
  `@modelcontextprotocol/sdk 1.29.0` 原本把 Zod Tool Schema 显式发现为
  Draft-07，现通过受控依赖补丁统一输出 JSON Schema 2020-12；9 个审批 Tool
  同时通过命名空间 `_meta` 声明 R0/R1/R2、`direct/prepare/execute` 和方言，
  并全部显式 `openWorldHint: false`。官方 Streamable HTTP Client 契约测试逐项
  校验输入/输出、注解和元数据，确定性目录门禁把这些语义纳入 `catalogHash`。
  该仓库修复不替代 Claude、Kimi、Cursor 的正式 Token、授权读写、过期/重放
  与实体 WebAuthn UAT。
- 2026-07-29 已关闭 Issue #9 的模板/实例草稿无法修订缺口：
  `PUT /approvals/templates/:id` 只更新当前未发布修订，
  `PUT /approvals/instances/:id` 只允许发起人更新未提交草稿；两者均绑定可信
  租户与主体、强 `If-Match`、请求摘要幂等和事务 Outbox。模板编码/修订、实例
  模板快照及发布/提交后的事实不可修改；两个新增事件只携带定义或表单摘要，
  名称、标题和正文不进入事件、审计或幂等响应。PC 模板设计器在结果未知时保留
  原请求和幂等键，正文变化才生成新键；标准 MCP 不新增模板或表单写 Tool。
  应用、控制器与 Outbox 共 154 项专项测试通过；三个目标生产边界覆盖率分别为
  95.19%/91.06%/97.61%/96.18%、100%/100%/100%/100% 和
  100%/100%/100%/100%（语句/分支/函数/行），Web 49 项与 MCP 确定性目录
  自检继续通过。
  真实 Mongo 并发/回滚、PC/H5 弱网重放、代表性流程和安全 UAT 仍待外部验收。
- 2026-07-29 已加固 Issue #17 的 PC 审批决策边界：普通决策按钮只在服务端
  身份摘要包含 `erp:approval:task:decide` 且待办为 R1 运行态时呈现；响应丢失
  或结果未知后，在页面内存保留原实例、主体、动作、强版本与幂等键，只允许
  完整原请求重试，服务端明确拒绝或可信主体变化才清除。PC 与既有 H5、转交、
  加签入口统一采用相同失败分类，专项契约测试覆盖无 Scope、R2、非运行态及
  换实例/主体/动作拒绝。真实浏览器弱网、响应丢失和实体用户三步 UAT 仍待
  外部验收。
- 2026-07-29 已加固 Issue #8 的组织公开读取与创建入口：组织应用服务、REST
  图表/写响应和标准 MCP `get_org_chart` 统一复用冻结最小投影，不再输出
  `tenantId`、`createdAt`、`updatedAt`；MCP 输出 Schema 与浏览器运行时契约均
  严格拒绝未知字段。PC 只在可信身份摘要包含 `erp:org:master:write` 时显示创建
  入口，创建载荷走白名单构造器；网络、5xx、限流、处理中或响应契约异常导致结果
  未知时保留同一主体、正文和幂等键，仅允许原请求重试。真实浏览器故障注入、实体
  角色矩阵、组织管理员 UAT 与外部平台下发仍待现场验收。
- 2026-07-29 已加固 Issue #120 的 Talent Lifecycle 公开读取和 R2 服务触点
  入口：REST 列表、详情与写响应通过防御性投影移除四域内部授权期限、部门路由、
  任务证据、未展示的写关联键、租户和持久化字段；PC 严格校验全部公开对象，只按可信
  `read + touchpoint:write` 显示创建，关闭再绑定责任人或 `write_all`。创建与关闭
  遇到结果未知时冻结同一主体、目标、版本、正文与幂等键，只允许原请求重试。
  标准 MCP `talent_lifecycle_get` 同步使用严格枚举/时间 Schema 和防御性七字段
  只读投影，不新增写 Tool。真实浏览器故障注入、实体角色矩阵、跨责任人关闭及
  HR/员工关怀/校友 UAT 仍待现场验收。
- 2026-07-29 已加固 Issue #105 的营销官网公开边界：CMS 列表只返回最多
  500 项最小摘要，详情与路径语言、类型和 slug 反向绑定；API 与 Website 共用
  严格成功信封和有界纯 JSON 契约。验证码同时绑定精确 Origin 与实际 iframe
  Window，匿名线索只接受精确结果投影，并在网络、限流、5xx 或响应契约异常时
  保留原请求与幂等键。发布失效同步清理列表和详情标签，失效 Secret 畸形时失败
  关闭，并对事件正文执行 16 KiB/Fatal UTF-8/精确 Schema。Website 30 项、
  营销服务 63 项、营销入口 97 项专项测试通过，目标文件
  逐文件四维覆盖率均不低于 90%；真实 WAF、验证码、浏览器故障注入、正式域名
  和营销 UAT 仍待外部验收。标准 MCP 继续只读且复用同一应用服务。
- 2026-07-30 已关闭 Phase 5 三次性能容量证据虽要求性能、平台和安全三方签署，
  却只校验角色、证据 ID 和时间的可信根缺口：输入升级为
  `gaoq.phase5.capacity.v2`，三方分别使用独立 Ed25519 公钥逐次签署完整环境、
  commit、k6 工具链、部署清单、四镜像、数据集、1000 VU/API 阈值、1000 人
  工资容量、基础设施和观测工件。keyId 绑定 SPKI DER 摘要，三方 keyset 与
  Repository Variable `PERFORMANCE_SIGNER_KEYSET_SHA256` 绑定；三次运行必须
  保持相同职责主体和受信 keyset，但九份证据、意见和签名全部独立。伪签名、
  角色换钥、公钥/主体/证据复用、签后篡改、超时签署与跨运行职责漂移均失败
  关闭。仓库自测只生成临时密钥；三次生产等价压测、真实人员 IAM/KMS/HSM
  职责绑定、联合签署和 WORM 原始材料仍待外部验收。
- 2026-07-30 已关闭容灾 v3 虽具备七方独立验签，却没有供外部证据生产器锁定的
  确定性契约，以及运行手册漏列独立专业算薪断连面的缺口：
  输入升级为 `gaoq.phase5.resilience.v4`；`resilience:print-contract` 固定
  八个业务域、九类外部连接、七个签署角色、
  RPO/RTO/断连/追赶目标、签名编码和校验器/工作流组合摘要；Phase 5 语义门禁
  会实际执行该契约输出。专业算薪 Resource、独立授权服务器、发布镜像、平台/
  事件契约、四 Tool/两 Resource Template/两 Prompt 目录与专项工件均进入
  环境绑定和七方共同签名 payload；手册把七类共享事件回放和旧工资事实源隔离
  列入双向断连验收。MCP 联调证据入口同时把
  畸形 JSON 收敛为稳定失败码，禁止原生解析异常穿透。真实恢复环境、专业算薪
  沙箱、职责密钥、现场演练和 WORM 原始材料仍待外部验收。
- 2026-07-30 已关闭 Phase 5 DAST/ASVS 四方签署只校验角色、证据 ID 和时间，
  且主动扫描后没有受保护最终证据验收链的缺口：输入证据升级为
  `gaoq.phase5.dast-asvs.v2`，AppSec、平台、QA 和风险四方分别使用独立
  Ed25519 公钥签署完整环境、目标摘要、commit、四镜像、ZAP/ASVS 工具链、
  L2/L3 控制、两轮报告、授权探针、安全断言和工件。keyId 绑定 SPKI DER
  摘要，完整角色/keyId 集合与 Repository Variable 的 signer keyset 摘要
  绑定；伪签名、角色换钥、主体/公钥复用、签后篡改、超时签署与 keyset 漂移
  均失败关闭。独立 `phase-5-dast-evidence` GitHub Hosted 工作流通过 OIDC
  拉取最终脱敏证据并绑定当前发布候选，只上传 verdict。仓库自测仅生成临时
  密钥；真实生产等价扫描、人工 ASVS、IAM/KMS/HSM 职责绑定、签署和 WORM
  原始材料仍待外部验收。
- 2026-07-30 已关闭最终发布门禁只校验 ERP MCP、却未绑定独立专业算薪资源
  服务器以及四方批准不可验签的缺口：`integration-mcp` 证据升级为 v3，新增
  专业算薪 HTTPS
  resource、独立授权服务器、镜像摘要、平台契约 `1.0.0`、七类事件契约摘要、
  MCP `catalogHash`、四 Tool/两 Resource Template/两 Prompt、三类客户端、
  跨 resource Token/错误租户拒绝和事件回放证据；九类外部沙箱均须进入同一
  verdict。集成、MCP、QA 和安全四方分别使用独立 Ed25519 公钥签署完整联调
  结果，keyId 绑定 SPKI DER 摘要，完整角色/keyId 集合再与 Repository
  Variable 的 signer keyset 摘要绑定；伪签名、角色换钥、主体/公钥复用、
  签后篡改、超时签署与 keyset 漂移均失败关闭。容灾证据把专业算薪作为第九类
  连接执行两小时断连、
  自动追赶和零差异对账，并绑定独立镜像；最终 Go/No-Go 精确绑定上述字段。
  受保护工作流只从 Repository Variables 取得非敏感预期值。该门禁不证明专业
  算薪已联调；仓库自测只生成临时签名密钥，真实人员、IAM/KMS/HSM 角色绑定、
  OAuth、MCP、事件回放、断连恢复、密钥轮换、两个影子周期和员工/财务 UAT
  仍待外部验收。
- 2026-07-30 已关闭最终 Go/No-Go 十方批准仅校验角色、证据 ID 与摘要格式，
  无法验证批准角色密钥的缺口：输入证据现为 `gaoq.phase5.go-no-go.v3`，
  架构、数据、财务、HR、法务、产品、项目发起人、QA、安全和 SRE 各使用独立
  Ed25519 公钥；keyId 必须等于 SPKI DER 摘要，完整角色/keyId 集合再与
  Repository Variable 的批准 keyset 摘要绑定。每份签名覆盖同一最终决策
  payload 摘要及自身角色、决定、证据、意见和时间；该 payload 覆盖版本、环境、
  十二门禁、量化验收、十一类外部集成、双方 MCP、运行保障与切换窗口。签名、
  payload、角色映射、复用公钥或签后篡改任一异常均失败关闭；Phase 6 Plan/Apply
  会重新验证相同 keyset。v3 还强制容灾与 MCP 联调引用同一专业算薪 Resource、
  授权服务器、镜像、目录和事件契约，拒绝跨环境或旧替身拼接。仓库自测生成
  临时测试密钥，不保存任何私钥；真实十方
  密钥托管、人员身份与角色密钥绑定、签署和 WORM 归档仍待现场执行。
- 2026-07-30 已关闭 Phase 5 七类 Readiness verdict 虽要求工程、供应链、镜像、
  权限、业务 UAT、隐私和运行负责人签署，却只校验角色、证据 ID 和时间的可信根
  缺口：输入证据升级为 `gaoq.phase5.readiness.v2`，13 个治理角色分别使用唯一
  Ed25519 公钥逐 Gate 签署完整业务证据、环境、commit、四镜像、部署清单、签署
  元数据和完整 keyset。同一角色跨 Gate 必须保持同一主体和角色密钥，不同角色
  不得复用主体、公钥、证据或签名；Repository Variable
  `READINESS_SIGNER_KEYSET_SHA256` 固定生产批准 keyset。伪签名、角色换钥、签后
  篡改、主体漂移或 keyset 漂移均失败关闭。仓库自测只生成临时密钥；真实人员、
  IAM/KMS 角色绑定、签署和 WORM 原始证据仍待现场验收。
- 2026-07-30 已关闭 Phase 5 容灾恢复与外部断连演练的七方批准只校验角色、
  证据 ID、意见摘要和时间，无法证明真实职责主体签署完整演练结果的缺口：
  输入证据现为 `gaoq.phase5.resilience.v4`，业务连续性、数据、集成、平台、
  QA、安全和 SRE 各使用独立 Ed25519 公钥。每个 keyId 必须等于 SPKI DER
  摘要，角色/keyId 规范集合与 Repository Variable 中批准的 signer keyset
  摘要绑定；共同批准 payload 覆盖环境、commit、五类镜像、RPO/RTO、恢复、
  回滚、八域与消息对账、专业算薪 Resource/授权服务器/目录/事件契约、九类
  外部连接、安全断言、产物和七方批准元数据。
  伪签名、角色换钥、主体或公钥复用、签后篡改、超时签署与 keyset 漂移均
  失败关闭。仓库自测只生成临时密钥；真实人员、IAM/KMS 角色绑定、现场演练、
  签署和 WORM 原始证据仍待外部验收。
- 2026-07-30 已关闭 Phase 5 三次全量迁移演练的架构、业务、数据和安全四方
  签署只校验角色、证据 ID 与时间，无法验证职责主体或完整迁移结果的可信根
  缺口：聚合证据升级为 `gaoq.phase5.migration-rehearsal.v2`，四方分别使用
  独立 Ed25519 公钥；keyId 等于 SPKI DER 摘要，角色/keyId 规范集合与
  Repository Variable 的批准 signer keyset 摘要绑定。共同批准 payload 覆盖
  环境、commit、四类镜像、部署清单、来源快照和包、三轮运行、全部 Scope、
  三类故障演练、安全结论及四方批准元数据。伪签名、角色换钥、主体或公钥复用、
  签后篡改、超时签署与 keyset 漂移均失败关闭。仓库自测只生成临时密钥；
  真实人员、IAM/KMS 角色绑定、三次现场演练、签署和 WORM 原始证据仍待验收。
- 2026-07-30 已关闭 Phase 6 生产 Apply 虽要求变更负责人和 SRE 双人批准、
  却只使用一个 Ed25519 密钥签署整份文件的职责分离缺口：部署授权升级为
  `gaoq.phase6.deployment-authorization.v2`，两个角色必须携带不同 SPKI DER
  公钥并分别签署同一完整授权 payload；keyId 等于公钥摘要，角色/keyId 规范
  集合还与 Repository Variable 的批准 keyset 摘要绑定。签名共同覆盖 Plan
  run/产物、commit、输入、渲染清单、目标集群、命名空间、两份批准元数据和最终
  决定；复用公钥、角色换签、签后篡改或 keyset 漂移均失败关闭。仓库自测只生成
  临时密钥，不保存私钥；真实人员身份、职责与角色密钥绑定仍须由企业 IAM/KMS
  和 WORM 签署审计证明。
- 2026-07-30 已关闭 Phase 6 平台准入声称变更、合规、数据、平台、安全、SRE
  六方签署，却只校验主体/证据摘要而无法验签的缺口：准入证据升级为
  `gaoq.phase6.production-platform-intake.v2`，六个角色必须使用六个不同
  Ed25519 公钥，分别签署同一最终准入 payload；keyId 等于 SPKI DER 摘要，
  角色/keyId 规范集合与 Repository Variable 的批准 keyset 摘要绑定。签名覆盖
  commit、部署包、护栏、集群、GitHub/OIDC/RBAC、九类平台服务、六份审批元数据
  和最终 `READY` 决定；伪签名、复用公钥、角色换签、签后篡改或 keyset 漂移
  均失败关闭，Plan 与 Apply 都重复验收。仓库自测只使用临时密钥；真实六方人员、
  IAM/KMS 角色密钥和 WORM 审计仍待现场验收。
- 2026-07-30 已关闭 Phase 6 只绑定 ConfigMap 名称、不能证明生产运行配置内容
  与专业算薪边界仍为批准版本的缺口：API/Worker ConfigMap 现在必须不可变，
  Plan 与 Apply 只读规范 JSON 并拒绝敏感键，显式校验
  `NODE_ENV=production`、各自 `RUNTIME_ROLE` 和
  `PAYROLL_SYSTEM_MODE=external`。API OAuth 额外 Resource 必须包含同一
  Go/No-Go 专业算薪 Resource；运行契约摘要同时绑定两份配置摘要、专业算薪
  Resource、七类事件契约和平台契约 `1.0.0`，并进入 Deployment/Pod annotation、
  values schema、渲染门禁和脱敏 artifact。Phase 6 还补齐了此前遗漏的五项
  Go/No-Go 专业算薪环境绑定，否则现场重验会在 Runner 内失败。该仓库门禁不读取
  Secret，也不替代真实 OAuth、事件回放、连接器部署或目标集群验收。
- 2026-07-30 已关闭统一切换五方验收及 Hypercare 归档三方批准仅校验角色、
  证据 ID、摘要和时间，无法证明职责主体真实签署的缺口：切换证据升级为
  `gaoq.phase6.cutover.v2`，业务、变更、数据、安全和 SRE 五方分别使用不同
  Ed25519 公钥签署同一完整切换终态；归档证据升级为
  `gaoq.phase6.hypercare-archive.v2`，数据、财务和法务三方分别签署完整 28 日
  稳定期及归档批准 payload。两条证据链都要求 keyId 等于 SPKI DER 摘要，
  角色/keyId 集合与各自 Repository Variable 的批准 keyset 摘要匹配，并拒绝
  主体/公钥/证据复用、角色换钥、签后篡改、伪签名、未来签名和 keyset 漂移。
  归档实际结果保持在批准 payload 之外，并单独强制发生在全部有效签名之后、
  只读、不可变且不授权删除。仓库自测只生成临时密钥；真实人员、IAM/KMS
  角色密钥、现场签署、WORM、切换和连续 28 日运行仍待外部验收。
- 2026-07-29 已把覆盖率门禁本身纳入失败关闭治理：
  `scripts/validate-critical-coverage-policy.mjs` 从 `precheck/check` 解析 134 个
  ERP API 专项脚本，展开其 `--coverage.include` 后要求 337 个生产文件逐一具有
  四维 90% 阈值、路径真实存在且专项清单完全闭合；同时把租户、Identity、
  Approval、Payroll、Treasury 与 MCP 六类章程关键域解析为 129 个权威文件，
  新文件不得逃出专项门禁，并锁定全生产源码 `src/**/*.ts` 四维 80% 分母。
  本轮补齐了 34 个此前仅在全量报告中的关键文件以及租户上下文、招聘渠道人工
  运维三个组合报告文件，负向自测覆盖分母、阈值、专项遗漏、关键域分类遗漏和
  生命周期断开。该仓库门禁已由 2026-08-01 的 `main` Hosted Actions 实际执行，
  但仍不替代 Phase 0 人工签署或目标环境验收。
- 2026-07-27 已冻结独立专业算薪边界：ERP 负责统一身份与组织主数据，专业算薪
  负责工资唯一事实源；`PAYROLL_SYSTEM_MODE=external` 关闭 ERP 旧工资/资金 REST。
- 2026-07-29 已关闭 Issue #30 的管理分析 Worker 旁路：驾驶舱 REST、标准 MCP
  和 R2 异步导出统一通过 `LegacyPayrollDashboardSource`；默认 `external`
  模式在构造 Mongo 查询前停止，返回空薪资聚合/新鲜度并从来源声明移除
  `payroll_periods`，只有显式 `legacy` 兼容模式可读取旧工资期最小投影。该仓库
  边界不替代专业算薪 OAuth/MCP、共享事件、两个影子周期与财务 UAT。
- 2026-07-29 已把上述边界下沉到 REST 与标准 MCP 共用的本人薪资单应用服务：
  `external` 模式在身份画像、Mongo 与 L4 解密前稳定失败关闭，MCP 返回
  `PAYROLL_MOVED_TO_PROFESSIONAL_SYSTEM` 并记录 R1 拒绝审计；兼容模式再次
  反向绑定可信主体、员工画像、期间、运行与结果快照，严格校验密文信封、解密
  Schema、确定性重算与摘要，仅返回冻结最小投影。66 项专项测试使目标服务四维
  100%，MCP Tool/Runtime 门禁继续高于 90%；真实专业算薪必须自行提供以用户
  OAuth 身份解析 `employee_id` 的 MCP/Resource Server，ERP 不代持或透传上游
  Token。该真实联调、密钥轮换和员工 UAT 仍待外部验收。
- 2026-07-29 已将旧系统模式判断收敛为共享
  `LegacyPayrollBoundaryService`，REST Guard、本人薪资单和 Treasury 银行账户
  在线登记/历史迁移复用同一边界。资金账户纵切先校验可信身份与 Scope，再在
  `external` 模式于审批、员工、Mongo、盲索引和加密前稳定失败关闭；兼容路径
  绑定专用已通过审批表单、活动事务、历史证据、连续版本、数据库投影/写回及
  加密信封。74 项资金账户测试达到 94.55%/93.56%/100%/96.12%，共享边界
  服务与 Guard 四维 100%，独立门禁已接入 `pnpm check`。标准 MCP 不新增银行
  账户能力；本切片不替代真实专业算薪、银行、迁移演练和财务 UAT。
- 2026-07-29 已把共享旧系统边界扩展到全部 Treasury 应用服务。代发在线提交、
  迁移导入、批准、制备及内部物化续跑，回盘在线/迁移导入，失败恢复和 Treasury
  四方对账在线/迁移入口均先校验可信主体及最小 Scope，再在强认证、生产授权、
  Payroll 端口、Mongo、加密、WORM、银行网关或 Inbox 前失败关闭。银行账户、
  代发、回盘、恢复、对账专项分别为 74、23、38、16、11 项，目标服务四维覆盖率
  均超过 90%，独立门禁已接入 `pnpm check`。真实专业算薪、银行、迁移演练和
  财务 UAT 仍待外部验收。
- 2026-07-29 已完成全部旧 Payroll 应用服务的共享系统模式边界下沉。算薪运行、
  审批、主数据、税务、Payroll 对账和影子周期共 31 个公开入口均自行先校验可信
  主体与最小 Scope，再在输入解释、幂等、强认证、Mongo、加密、WORM、税务网关
  或跨域读取前失败关闭；标准 MCP 只读、迁移及 Treasury 内部调用不能绕过。
  六组专项共 157 项测试通过，目标服务覆盖率依次为
  92.36%/90.20%/96.10%/93.37%、100%/100%/100%/100%、
  100%/100%/100%/100%、98.90%/98.58%/100%/100%、
  96.58%/96.82%/100%/99% 和 99.03%/97.24%/97.29%/98.88%
  （语句/分支/函数/行）。本人薪资单和共享边界既有门禁继续四维 100%。
  本地代码门禁不替代真实专业算薪、税务/银行、历史迁移、两个影子周期和财务
  UAT。
- 2026-07-29 已补齐晚于上述基线合入的工资调账链边界：调账主服务、负向员工
  应收、调账税务更正、年度汇算、Treasury 正向补发及调账候选重算均在可信身份
  授权后、任何旧 Mongo/强认证/生产授权/WORM/税局/银行动作前复用共享边界。
  新增仓库门禁扫描 17 个旧 Payroll/Treasury 应用服务与 71 个公开异步入口，
  漏注入或漏校验会阻断 `precheck/check`；8 个定向测试文件 266 项通过。
  标准 MCP 契约未扩大，仍只复用应用服务读取最小控制摘要。真实专业算薪、税局、
  银行、影子周期与财务 UAT 仍待外部验收。
- 2026-07-27 已将 ERP 与专业算薪共享契约提升至
  `@gaoq/platform-contracts@1.0.0`：七类事件统一为
  `cn.gaoq.<域>.<实体>.<动作>.v1`，完整信封和逐类型负载严格校验，并同步导出
  JSON Schema；旧 `com.gaoq.*` 名称只保留一个迭代的显式严格迁移入口。
- 强制规范入口为 `docs/phase-0/README.md`；阶段交付与未验收边界分别见
  `docs/phase-1/README.md` 至 `docs/phase-6/README.md`。
- 2026-07-27 已在本机专用 MongoDB Replica Set 与 Redis 上完成 API、Worker、
  Web、OAuth Client Credentials、官方 MCP SDK、CORS、队列和指标端点联调；
  该结果属于本地运行验证，不替代现场验收。
- 2026-07-29 已加固 ERP→钉钉/飞书员工首次平台开户：Worker 认领后严格复核
  任务、敏感资料有效期、确定性平台 userId 和全部身份标识；AccessProfile、
  ExternalIdentity、考勤映射及请求终态保持同事务，提交后会话清理或审计故障
  不再进入通用失败回写。考勤外部员工映射另强制活动事务、员工与外部标识双向
  唯一查询、最小投影反向绑定、盲索引/密文信封和写回结果校验；冲突直接进入
  人工复核，不再依赖唯一索引后无界重试。R3 入口继续拒绝 MCP 主体且不注册开户
  写 Tool。124 项专项测试达到 99.02%/97.69%/100%/100%，八个生产文件逐文件
  四维均不低于 90%；真实钉钉/飞书沙箱、Secret 轮换和人工身份核验仍待现场验收。
- 2026-07-29 已加固 HC→审批→职位管理入口：七个 REST 端点严格限制 ULID、
  白名单幂等键、不会上溢的强 ETag、精确 DTO 和无正文动作；领域模型再次校验
  HC 理由、状态枚举、安全版本、单调时间与迁移快照。业务失败按动作记录低敏
  R1/R2 审计，提交后审计异常只作稳定告警。97 项入口/领域专项测试使 REST/DTO
  合计四维 100%，职位领域达到 100%/97.26%/100%/100%，HC 领域达到
  92.30%/90.16%/100%/95%，共享校验四维 100%；标准 MCP 契约不变，仍只复用
  应用服务执行 HC 提交 R2 与职位迁移 R1，不接收审批终态或 L3 原文。真实
  Approval、组织主数据、招聘渠道回放和 HR UAT 仍待现场验收。
- 2026-07-29 已加固面试排期、评价、完成与取消入口：四个写端点严格限制 ULID、
  白名单幂等键、不会上溢的强 ETag、精确 DTO 或空正文；排期只接受规范毫秒级
  UTC 与运行时可解析 IANA 时区，评价不得早于开始，完成不得早于结束。领域模型
  再次校验状态、版本、面试官证据、单调时间及精确迁移评价对象，拒绝存取器、
  Symbol、代理和自定义原型。业务失败写低敏 R1 审计，提交后审计异常只告警。
  158 项专项测试使应用服务四维 100%、REST/DTO 四维 100%、领域达到
  98.74%/98.64%/100%/100%；四个生产文件逐文件四维均不低于 90%。标准 MCP
  仍只复用应用服务返回脱敏面试摘要，不开放排期、评价、完成、取消、日历处置或
  L3 原文。真实钉钉/飞书沙箱、代表性时区、招聘经理流程和 HR UAT 仍待现场验收。
- 2026-07-28 已加固招聘面试日历向钉钉/飞书的可靠投递：只自动认领
  `pending`，过期 `processing` 隔离为结果不确定；不可变事件版本与当前投影
  严格核对，飞书多步写入保留部分提交事件标识，平台成功后的本地终态或审计
  故障不再进入通用失败回写。人工处置 REST 强制可信租户、幂等、R2 审计及
  `approved_exception`；标准 MCP 继续只读，不暴露日历写入、重试、对账、
  外部标识或凭据。169 项专项测试达到 99.16%/97.79%/100%/99.76%，九个
  生产文件逐文件四维均不低于 90%；真实平台沙箱和人工核验流程待现场验收。
- 2026-07-28 已补齐 eSign 发起状态机：ERP 用户请求先持久化加密意图，再由
  可信 Worker 调用供应商；结果未知永久进入人工核验，已取得外部 flowId 时只补
  本地终态。人工重试要求批准例外及供应商确认未创建，人工绑定不能直接写成功；
  完成 Job 立即清除以允许处置后复用确定性 JobId。139 项专项测试达到
  97.95%/94.98%/100%/98.74%，五个目标生产文件逐文件四维均不低于 90%；
  标准 MCP 永久不开放发起、重试、人工处置、外部标识或签署主体。真实 eSign
  租户、实名认证、人工核验和业务 UAT 仍待现场验收。
- 2026-07-27 已交付 `/careers` 招聘门户、ERP 开放职位最小投影、候选人联系人
  投递 BFF、可信入口验证、精确 Origin 和共享 Redis 失败关闭限流；生产服务身份、
  边缘代理注入、简历原件扫描/对象存储仍待现场配置与验收。
- 2026-07-27 已交付智能简历库：隔离网关脱敏读取契约、OpenAI 严格结构化输出、
  受控分类标签、BullMQ Worker、人工确认与标签检索、ERP 管理页面及独立索引迁移；
  真实附件网关、OpenAI Secret/数据保留控制、代表性简历评测与招聘 UAT 待现场验收。
- 2026-07-28 已加固智能简历外部连接与异步恢复：来源网关严格 HTTPS、租户回显、
  256 KiB 有界 JSON 与 NFKC 后直接标识检查；OpenAI 请求强制 `store:false`、
  严格 Schema、哈希 `safety_identifier`，拒答/未完成响应失败关闭；BullMQ 使用
  租户绑定确定性 JobId 和版本租约。REST、Worker、队列与适配器已纳入逐文件 90%
  门禁；真实网关和 OpenAI 项目策略仍待现场验收。
- 2026-07-28 已加固招聘渠道补拉边界：Mongo 回读绑定二次闭合可信租户、绑定
  ULID、活动状态、渠道装配和 Secret 引用；第三方批量结果、投递 Envelope、
  事件 ID、毫秒 UTC 时间、批内唯一事件及前进游标在任一 Inbox 写入前整批校验。
  原始载荷只接受有界纯 JSON，并按单条 512 KiB、整批 4 MiB、深度、节点、数组
  和对象键预算失败关闭，通过后只传递规范深冻结副本。85 项专项测试使 Registry
  与补拉服务合计达到 98.22%/95.47%/100%/98.40%（语句/分支/函数/行），两个
  文件逐文件四维均不低于 90%；标准 MCP 不新增补拉、游标、Inbox、凭据、证据
  处置或重放能力，真实渠道 fixture、限流和 UAT 仍待现场验收。
- 2026-07-28 已加固招聘渠道出站副作用：职位发布/下架与阶段回传只认领
  `pending`，过期租约、未分类传输异常及外部响应后的本地终态故障进入
  `manual_review`；只有 Adapter 明确声明 `not_committed` 才允许自动重试。
  受控人工处置 REST 强制可信租户、幂等键、R2 审计，并要求批准例外与供应商
  确认未提交；标准 MCP 不开放人工核验、重试或渠道凭据。真实渠道沙箱和处置
  UAT 仍待现场验收。
- 2026-07-27 已交付 Talent Lifecycle 360：以候选人身份为主线实时组装招聘、入职、
  任职、离职与校友状态，提供加密服务触点、授权门禁、Outbox、只读 MCP 和管理页面；
  生产索引、角色映射、历史数据回放及 HR/员工关怀/校友 UAT 待现场执行。
- 2026-07-27 已交付 Knowledge 可靠考试编排：版本化题型/时限/次数/评分与通过
  策略，独立签名评分网关，超时自动提交、人工复核、Mongo 权威状态机、Worker
  退避/死信/熔断、事务 Outbox、SLA 指标、追加索引、只读对账、显式重放及本人
  只读 MCP；ERP 与 MCP 均不接收题目、答案或标准答案。真实评分沙箱、代表题集、
  性能、安全及培训 UAT 仍待外部验收。
- 2026-07-29 已加固 Knowledge 考试运行持久化与事务事件边界：仓储输入、可信
  租户、最小投影、考试/任务反向绑定、题型策略、状态、版本、锁、重放和时间/
  证据组合均执行运行时校验；尝试号、插入和提交 CAS 强制活动 Mongo 事务并验证
  写回终态。Knowledge Outbox 对课程、任务、考试运行、评分和入职证明 15 类事件
  使用逐类型负载白名单，拒绝答案、标准答案、Token、未知字段与状态错配。124 项
  专项测试使两个生产文件达到 95.71%/94.06%/100%/97.42%，逐文件四维均不低于
  90%；标准 MCP 仍只读本人脱敏状态，不直连仓储或 Outbox。
- 2026-07-27 已交付本人授权知识全文检索：可信任职及部门/岗位裁剪、应用服务二次
  授权、独立签名搜索网关、事务索引任务、重试/死信、新鲜度指标、追加迁移、只读
  对账、全量重建及标准 MCP；真实搜索集群性能、安全、撤权收敛与业务 UAT 待现场验收。
- 2026-07-29 已加固 Knowledge 搜索索引事务任务写入边界：仅接受规范事件
  ULID、可信租户、课程版本和规范授权投影，固定已发布 `upsert`、已下架
  `delete`；部门/岗位集合要求安全、唯一、稳定排序且有界。写入强制活动 Mongo
  事务并反向绑定全部数据库创建回执与初始待处理状态。47 项专项测试使目标 Writer
  达到 100%/98.24%/100%/100%，独立四维 90% 门禁已接入持久化组合门禁；
  REST、事件和标准 MCP 契约不变，MCP 不直连任务集合或搜索网关。
- 2026-07-28 已加固 Knowledge REST 写入口：课程发布校验只在幂等事务的新执行
  分支内针对精确课程版本发生，已完成请求直接重放快照，不再受外部校验器可用性
  影响；Mongo 自动重试相同快照时复用校验结果，课程事实变化则重新校验并拒绝旧
  版本写入。业务提交后的审计异常只形成稳定告警，不再把课程或培训任务成功终态
  暴露为失败，R0 读取审计仍保持失败关闭。
- 2026-07-27 已补齐校友授权自动到期：创建时使用可信租户与授权标识生成稳定
  BullMQ 延迟任务，到期 Worker 仅持有内部 expire Scope，发布脱敏事件并写审计；
  存量活动授权提供默认 dry-run、显式 apply 的稳定 JobId 批量重建工具，且不向
  REST 或 MCP 暴露执行能力。
- 2026-07-27 已交付生日与入职周年关怀编排：Person 生日月日只形成独立密钥域
  HMAC 盲索引，Care 通过窄应用服务读取在职事实；本人偏好/全局退订、租户时区、
  闰日与复聘策略、静默时段、受控模板、事务 Outbox、稳定 BullMQ 任务、签名通知
  回执、锁恢复/退避/死信/显式重放、追加迁移、低基数指标及标准只读 MCP 已落地。
  真实通知沙箱、渠道授权映射、代表性时区数据与员工 UAT 仍待现场验收。
- 2026-07-29 已加固自然人生日证明 L4 写入口：REST 严格校验 ULID、强
  `If-Match`、白名单幂等键、规范日历月日和未知字段；应用服务再次复核可信服务
  主体、Scope、请求结构及幂等结果对象/版本闭包。业务失败审计故障不覆盖原异常，
  提交后的成功审计故障只形成低敏告警，响应、审计和日志均不含月日或证据标识。
  73 项专项测试使 DTO、应用服务和控制器达到 100%/100%/100%/100%，独立
  四维 90% 门禁已接入 `pnpm precheck` 与 `pnpm check`；标准 MCP 不新增生日
  登记、盲索引解析或证明读取能力。
- 2026-07-27 已交付校友授权终止后的下游清理证明闭环：撤回与到期共用可靠
  状态机，按独立目标/凭据/Ed25519 信任根验证不可变证明，具备策略版本幂等、
  Worker 锁恢复/退避/死信、只读对账、审批重放、缺失任务重建、追加索引、
  低基数指标、REST 与标准只读 MCP。真实 CRM、通知、校友平台删除和未来处理
  阻断、WORM 保留、故障演练及业务 UAT 仍待现场验收。
- 2026-07-28 已加固校友授权清理证明出站边界：运行时重算任务 ID 与控制摘要，
  复核执行租约、目标政策及互不复用的 Origin、凭据、Ed25519 Key ID/公钥；固定
  HTTPS `POST /v1/alumni-consent-cleanups/execute` 和最小协议 Header。非 200
  不读取正文；成功证明执行无压缩严格 JSON、规范 Content-Length、16 KiB 流式
  上限、原始字节验签、Fatal UTF-8、完整 Schema、上下文、存储引用和最低保留期
  绑定。131 项专项测试达到 96.39%/96.15%/96.42%/97.59%
  （语句/分支/函数/行），三个生产文件逐文件四维均不低于 90%；MCP 继续只读
  脱敏状态计数，真实 CRM/通知/校友平台、WORM 与凭据轮换仍待现场验收。
- 2026-07-27 已交付双语营销官网、ERP 工作台 CMS、事务 Outbox、通知幂等、
  严格生产 HTTPS/CSP、安全头、独立镜像与加密预约线索入口；对象存储、真实通知、
  验证码、AI 网关和正式域名仍待现场配置与验收。
- 2026-07-27 已补齐 Website 生产失败关闭构建、安全响应头、精确 CORS/验证码
  协议、独立非 root 镜像与供应链扫描，以及 Deployment、Service、Ingress、
  NetworkPolicy、HPA、PDB、探针和构建/运行配置边界静态门禁；真实域名、证书、
  WAF、验证码和对象存储仍需目标环境证据。
- 2026-07-27 已补齐营销副作用 `dispatched → delivered|dead|cancelled` 终态、
  送达尝试计数、最终失败告警、撤销排期事务取消、跨租户队列路由拒绝及从数据库
  Outbox 重建丢失延迟任务；真实邮件/飞书幂等与故障恢复仍待目标环境演练。
- 2026-07-28 已加固营销副作用可靠投递：运行时严格校验 Outbox 路由事实，受损
  记录立即隔离；认领与释放绑定 Worker、状态和尝试次数，租约丢失失败关闭；通知
  网关幂等键绑定唯一 eventId，网关成功后的本地终态故障不再被误记为发送失败。
  到期排期修复新增专用索引和 v2 追加迁移，营销 MCP 继续保持只读。
- 2026-07-29 已加固 Issue #106 的营销 CMS 管理入口：Controller 对内容、
  修订、线索、媒体与 AI 结果执行显式最小公开投影，剥离租户、内部维护者、归因、
  备注、对象引用、扫描证据、模型和提示词版本；PC 先校验可信身份摘要，只按
  `read + action` Scope 显示入口，并对全部响应执行精确键、枚举、时间、HTTPS
  URL 和有界纯 JSON 校验。内容流转/排期/回滚、线索状态、媒体票据/直传/核验和
  AI 生成/复核在结果未知时保留同一主体、目标、版本、正文与幂等键，只允许原请求
  重试。AI 与媒体网关拒绝可执行/无界输出和非 HTTPS 能力 URL；标准 MCP
  `marketing_side_effect_get` 防御性重建十二字段冻结投影，输入输出 Schema
  拒绝未知字段并严格约束标识、时间和错误码，不新增写 Tool。真实对象存储、AI、
  邮件/飞书、浏览器故障注入、实体角色矩阵和营销 UAT 仍待外部验收。
- 2026-07-28 已加固审批通知双平台可靠投递：认领事实、任务载荷、状态写入和租约
  释放均执行运行时校验并失败关闭；飞书以通知 ULID 作为 `uuid` 安全恢复，钉钉
  对过期租约及不可判定响应隔离为结果不确定死信，只有完成平台对账后才允许 R2
  `approved_exception` 恢复。平台成功后的本地终态故障单独记录
  `state_unavailable`，不得反向登记发送失败；现有审批 MCP 保持标准只读能力，
  不新增通知重试写 Tool。
- 2026-07-29 已加固审批通知 R2 运维边界：Controller 严格拒绝未知正文、非规范
  ULID、分页和幂等键，应用服务再次校验读/操作 Scope；死信、重试终态和对账
  最老积压均使用最小投影并反向绑定可信租户、状态、错误、计数和时间。四类人工
  恢复原因只能匹配各自错误类别，最多执行 100 次；业务失败审计故障不覆盖原始
  异常，事务提交后的成功审计故障不改变成功响应。77 项专项测试使 Controller 与
  应用服务达到 100%/99.02%/100%/100%，独立逐文件四维 90% 门禁已接入
  `pnpm precheck` 和 `pnpm check`；`phase-2-indexes-v3` 以独立迁移记录追加
  租户/状态/通知游标复合索引，审批 MCP 仍不开放异常队列、对账或重试。
- 2026-07-28 已加固 ERP 组织主数据向钉钉、飞书和 OP 的可靠投递：过期
  `processing` 不再自动重放；已提交版本只补写成功，未确认结果进入
  `manual_review`；平台已受理后的本地终态故障单独记录 `state_unavailable`。
  投递与对账终态写入均绑定原租约并检查 `matchedCount`，结果不确定记录只有完成
  平台核验后的 R2 `approved_exception` 可恢复。Prometheus 告警和四维 90%
  专项门禁已交付；MCP 继续只开放复用组织应用服务的 `get_org_chart`，不暴露组织
  投递或重试写能力。
- 2026-07-29 已闭合组织投递、招聘日历和员工首次开户三个 Integration 运维
  REST 的提交后审计边界：未知请求字段、非规范分页数字、非法原因/决策均在应用
  服务前失败关闭；业务失败后的审计故障不覆盖原始异常，业务已提交后的成功审计
  故障只形成稳定低敏告警，不改变成功响应。组织投递 Controller 的 18 项测试
  达到四维 100%，并纳入既有组织投递可靠性逐文件 90% 门禁；标准 MCP 仍不开放
  投递恢复、日历处置、员工开户或平台凭据。
- 2026-07-29 已加固组织下发与招聘日历共用的外部身份解析边界：调用参数、活动
  平台绑定和 bound 身份最小投影均执行严格运行时校验；钉钉访问令牌的外部租户
  必须反向绑定同一活动记录，`unionId → userid` 回执只接受规范标识。受损投影、
  未知字段、令牌投影异常和租户漂移在平台调用前以稳定低敏错误失败关闭，不再
  降级为正常“未绑定”。20 项专项测试使目标生产文件四维均为 100%，独立逐文件
  90% 门禁已接入 `pnpm precheck`；标准 MCP 不新增外部身份、令牌或平台写能力。
- 2026-07-28 已补齐组织平台连接边界的失败关闭证据：钉钉、飞书、OP 适配器及
  两类专用 HTTP 客户端覆盖白名单目标、路径与请求头约束、令牌单次刷新、HMAC
  原始字节签名、确定性身份恢复、分页/对象/响应体上限和稳定错误分类。88 项专项
  测试全部通过，五个生产文件合计达到 97.18%/95.18%/100%/97.99%，且每个文件
  四维均不低于 90%；MCP 边界不变，仍不暴露平台写入或凭据。
- 2026-07-28 已补齐 Attendance 规则纵切：不可变班次规则、唯一
  `Employment` 完整覆盖排班、员工级事务并发守卫、Provider 月末水位线与零未决 Inbox 覆盖证明、
  跨天签退归属及规则推导分钟均进入月结事务边界；REST、脱敏 Outbox、R2 审计、
  追加索引迁移和独立四维 90% 门禁同步交付。85 项专项测试达到
  98.61%/95.04%/100%/99.07%（语句/分支/函数/行）；标准 MCP 继续只读已关闭
  快照和准备本人修订，不开放规则、排班、对账或月结写能力。真实 Provider 沙箱、
  生产 Replica Set 索引和业务 UAT 仍待外部验收。
- 2026-07-28 已加固 Attendance 核心领域与 L4 加密：在线与迁移 UTC instant
  验证真实日历时间，业务日期拒绝不存在的日期，源观测时间被约束在事实发生与
  ERP 登记之间；加密信封在 Base64URL 解码前校验 IV/AuthTag 固定长度和密文
  编码上限。99 项专项测试达到 98.23%/97.43%/98.24%/98.85%
  （语句/分支/函数/行），两个生产文件逐文件四维均不低于 90%。
- 2026-07-28 已加固身份令牌与 OAuth 授权事务：JWT 强制
  `sub = tenant_id:actor_id`、拒绝重复授权集合并深冻结可信投影；授权请求展示、
  决策和授权码交换均按当前客户端注册表重验回调、resource、租户与 Scope，
  配置收紧后 Redis 旧记录失败关闭。39 项专项测试达到
  97.80%/93.10%/100%/100%（语句/分支/函数/行），两个生产文件逐文件四维
  均不低于 90%。
- 2026-07-28 已加固审批主体解析信任边界：发起人除 active 授权快照外必须仍为
  ERP active/probation 员工；角色、固定员工、部门负责人结果逐项复核可信租户、
  查询标识、状态与 actor/employee 唯一映射，持久化漂移失败关闭且单节点最多
  100 人。`department_manager` 模板只允许引用 department 类型字段。REST、OP
  与标准 MCP 继续复用同一审批应用服务，不接受 AI 自定义审批人。13 项专项测试
  达到 96.96%/95.60%/100%/98.79%（语句/分支/函数/行），目标生产文件逐文件
  四维均不低于 90%；真实组织角色数据、历史模板与审批 UAT 仍待外部验收。
- 2026-07-28 已加固生产运行入口与可观测性边界：API 就绪探针有界并行验证
  MongoDB 可写 Replica Set 主节点和 Redis PONG，并以单飞重连防止探针连接风暴；
  Prometheus Method/状态标签收敛到固定集合，未知异常和 5xx 隐藏正文，4xx 详情
  有数量/长度/控制字符上限。Worker 指标入口返回标准 Bearer challenge 与安全
  响应头，Redis URL 独立复核规范结构并支持 `rediss://`。11 个测试文件、40 项
  测试达到 99.70%/94.44%/100%/99.67%（语句/分支/函数/行），11 个生产文件
  逐文件四维均不低于 90%；本切片不新增 MCP 能力，真实主节点切换、TLS/ACL、
  Prometheus 抓取与告警仍待现场验收。
- 2026-07-28 已加固 ERP→OP 审批结果出站连接边界：专用客户端固定 HTTPS
  origin、PUT 路径和八个签名协议 Header，拒绝额外/重复 Header、控制字符及
  非规范签名材料；出站正文必须为 16 KiB 内 JSON 对象，响应按 Content-Length
  与流式读取实施 256 KiB 双重上限并严格解码 UTF-8/JSON。非 2xx 只按状态码
  分类且不保留上游正文或 cause；上游 requestId 仅接受有界可见 ASCII。49 项
  专项测试达到 99.13%/98.03%/100%/99.02%（语句/分支/函数/行），目标生产
  文件逐文件四维均不低于 90%；REST/事件/MCP 继续复用审批应用服务，不新增
  AI 写能力，真实 OP TLS、限流、幂等与 Secret 轮换仍待现场验收。
- 2026-07-28 已加固 ERP→银行支付提交信任边界：专用适配器固定标准 HTTPS
  `/v1/submissions`、POST 与协议 Header，运行时二次校验独立凭据、sandbox/
  production 模式和一次性短时生产授权。非 2xx 不读取银行正文；成功回执执行
  Content-Length 与 16 KiB 流式双重限长、严格 UTF-8/JSON、完整 Schema 及
  批次/对象/摘要/控制量/授权回显绑定，读取取消和释放异常只收敛为本域错误。
  71 项专项测试达到 99.01%/96.70%/100%/98.87%（语句/分支/函数/行），目标
  生产文件逐文件四维均不低于 90%；MCP 继续只读脱敏控制摘要，真实银行签名加密、
  沙箱联调和生产授权域验收仍待现场执行。
- 2026-07-28 已加固银行回盘 Inbox 入站信任边界：专用适配器固定标准 HTTPS
  `POST /v1/returns/claim`、协议 Header、独立凭据和确定性幂等键，只发送租户、
  ULID 批次与银行提交 ID。非 2xx 不读取正文；成功清单执行 Content-Length 与
  4 MiB 流式双重限长、严格 UTF-8/JSON、完整 Schema 及领取对象绑定，读取、
  取消和释放异常收敛为稳定本域错误；验签或恶意文件负面证据原样交由应用服务
  整批冻结。65 项专项测试达到 98.90%/96.15%/100%/98.70%
  （语句/分支/函数/行），目标生产文件逐文件四维均不低于 90%；MCP 继续禁止
  领取、上传或处理回盘正文，真实银行回盘验签、恶意文件和限流联调待现场验收。
- 2026-07-28 已加固 Payroll Tax 双出口信任边界：WORM 固定
  `POST /v1/objects`，在外呼前严格解码规范清单并绑定可信租户、申报 ULID、
  对象键和摘要；税务网关固定 `POST /v1/submissions`，只提交 WORM 引用、
  月份和控制量，production 二次校验 30 秒至 15 分钟短时独立授权。两个出口
  运行时复核独立凭据，非 2xx 不读取正文，成功回执执行 Content-Length 与
  16 KiB 流式双重限长、严格 UTF-8/JSON、完整 Schema 和请求事实绑定。131 项
  专项测试达到 99.35%/98.57%/100%/99.25%（语句/分支/函数/行），三个生产
  文件逐文件四维均不低于 90%；MCP 继续只读脱敏控制摘要，真实税务沙箱、
  WORM 锁定/保留证明、Secret 轮换和生产授权域仍待现场验收。
- 2026-07-28 已加固 Treasury WORM 证据出口：固定标准 HTTPS
  `POST /v1/objects`，运行时复核独立可见 ASCII 凭据、十年至一百年保留期、
  租户、批次 ULID、L4 分类和确定性幂等域。pain.001 在外呼前执行 Fatal UTF-8、
  固定 Schema、唯一 MsgId/PmtInfId、DOCTYPE/ENTITY 禁用、对象键与摘要绑定；
  非 2xx 不读取正文，成功回执执行严格 JSON、Content-Length、16 KiB 流式上限
  及租户/批次/对象/摘要/保留期反向绑定。98 项专项测试达到
  100%/97.77%/100%/100%（语句/分支/函数/行），独立逐文件四维 90% 门禁已由
  资金支付总门禁接入 `pnpm check`；MCP 不新增归档、文件读取或资金写能力，
  真实 Object Lock、保留期证明、凭据轮换、断连和限流仍待现场验收。
- 2026-07-28 已加固 OP 经营摘要可靠处理链路：BullMQ JobId 绑定租户、Inbox
  与载荷摘要，Worker 通过任务标识、随机令牌和 15 分钟租约栅栏处理并支持同任务
  恢复；事务提交后的终态写入与审计故障不再触发通用失败回写。REST 与 MCP 统一
  只返回日期、修订、币种和五项经营指标，应用服务重验可信主体与最小 Scope，
  Outbox 对信封和业务负载逐字段失败关闭。114 项专项测试达到
  99.34%/97.35%/100%/100%（语句/分支/函数/行），五个生产文件逐文件四维
  均不低于 90%；真实 OP 联调、故障演练和业务 UAT 仍待外部验收。
- 2026-07-28 已加固 Phase 5 管理驾驶舱与受控分析导出：历史进行中审批按
  submitted/completed 时间重建，最终通过率改用不可变 `approval_actions`；
  REST、MCP 与导出共用递归严格契约及应用服务二次 Scope 校验。BullMQ JobId
  绑定租户、发起人、导出 ULID 与 generation，Worker 以 JobId、随机 token 和
  五分钟租约 fencing 提交终态；业务终态后的审计故障不反向回写失败。52 项
  专项测试达到 99.59%/98.23%/100%/99.56%（语句/分支/函数/行），七个生产
  文件逐文件四维均不低于 90%；真实 OP 数据覆盖、生产 explain/容量测试和管理
  UAT 仍待外部验收。
- 用户已明确不使用 NAS、自建 Runner 或虚拟机，CI/CD 只采用 GitHub Hosted
  `ubuntu-latest`。2026-07-29 已把全部 Phase 5/6 手工验收和生产 Plan/Apply
  工作流从本地挂载迁移为 workflow/policy 专用 OIDC：三个失败关闭客户端逐项
  复核仓库、commit、workflow、policy、audience 与 Hosted Runner claims，
  通过批准的 HTTPS 网关读取固定摘要脱敏证据，并以不含静态 Token 的
  ExecCredential 接入 Kubernetes；Plan/Apply 使用不同 audience 和 RBAC Group。
  仓库当前为 GitHub Free Public，生产保护不把可见性或付费
  Environment/Required Reviewers 作为唯一控制；生产 Plan/Apply 已拆成不可自动
  串联的独立工作流，Apply 额外验收变更负责人
  与 SRE 使用不同批准密钥形成的两份 Ed25519 签名、Plan run/计划包和两小时
  有效期。全部 workflow 已无 self-hosted 标签。2026-08-01 的 `main` Hosted
  Actions 已实际通过 Phase 1、Phase 5 和文档门禁；生产 Plan/Apply 仍须在真实
  企业证据代理、OIDC/RBAC、职责签名和目标集群就绪后分别人工执行。
- 2026-07-29 已把 GitHub 元数据规范转为只读失败关闭门禁：
  `scripts/github/validate-repository-governance.mjs` 校验固定七个 Milestone、
  Issue 标签/状态/Epic 子项，以及 PR 唯一 Milestone、Ready 前 CR、真实 Issue
  关联、验证证据和非 `main` 来源分支；23 个负向自测已接入根 `test/check`。
  一次性治理收敛为 67 个历史 PR 补齐 Milestone、37 个历史 PR 补齐真实 Issue
  关联，并为 Issue #12 补齐阻塞与解除章节；本地 `gh` 实时校验 7/50/79
  （Milestone/Issue/PR）通过。专用 Hosted Actions 工作流只申请 contents、
  issues、pull-requests 读权限；2026-08-01 的 `main` push 因该工作流仅接受 PR
  事件而取消，不属于治理校验失败，后续治理 PR 必须以实际工作流结果为准。
- 2026-07-29 已按当前提交候选的实现、迁移、MCP 协议与专项覆盖证据同步
  Issue #123–#126：四个 Story 的仓库验收项已逐项标记完成，真实通知/CRM/
  校友渠道、搜索集群、评分服务、代表性数据和业务 UAT 继续保持未完成与
  `status:external-acceptance`。同时修正 Phase 4 完成度审计中把已交付的月中
  拆分、工资调整结算和年度核对写成“未实现”的过期表述，并由文档校验器拒绝
  该状态回退；官方个人综合所得申报仍属于外部法定系统职责。
- 2026-07-29 已补齐 Issue #6 的本地开发运行时：`pnpm dev:up` 自动探测
  Compose，启动并等待 MongoDB Replica Set、Redis、MinIO 对象存储及存储桶，
  随后统一监管 API、Worker、ERP Web 与官网；对象存储高熵凭据仅以 `0600`
  写入 Git 忽略目录，任一应用退出会收敛其余进程。`pnpm dev:down` 只停止
  容器并保留卷；本地 MinIO 明确不替代 WORM/KMS/扫描、GitHub CI 或生产验收。
- 2026-07-29 已补齐 Issue #11 的 ERP Web 基础无障碍仓库门禁：根布局增加
  主内容跳转，PC/H5 统一高可见焦点与减少动画策略，登录和组织入口显式暴露
  busy/alert 语义；独立 Vitest 固定语言、视口、地标、标题、导航名称、断点、
  traceId、可信服务端权限文案及禁止浏览器持久化身份数据。375px/768px/桌面
  实体浏览器、200% 缩放、读屏器和 WCAG 2.2 AA 扫描仍属于外部验收。
- 2026-07-27 已完成开放 Issue 与 Phase 0–6 仓库实施证据审计，并正式定义
  `status:implementation-delivered` 与 `status:external-acceptance` 的并存规则；
  新增 Phase 3 Issue 已归入唯一 Milestone。GitHub Project 仍由 Issue #41 跟踪，
  因当前令牌缺少最小 `project` 权限而保持阻塞；详见
  `docs/implementation-completion-audit.md`。
- 2026-07-30 ERP API 覆盖率已修正为显式纳入 `src/**/*.ts` 全部生产源码，
  包括测试未加载的启动、Worker、Controller、迁移和适配器文件，并把全量覆盖
  命令接入 `pnpm check`。补齐年度个税可信接入后的当前真实基线为
  语句 93.24%、分支 90.98%、函数 93.37%、行 94.27%，439 个测试文件、
  7,217 项测试全部通过并达到全仓四维 80% 强制门槛；完整 `pnpm check`、
  显式公开 Origin 的 `pnpm build` 与 `pnpm audit --audit-level high` 同日
  通过，依赖审计未发现已知漏洞。
- 2026-07-28 已加固 Talent Lifecycle 的 Recruitment、Onboarding、Org 与
  Care 四域来源完整性：每个窄查询口都二次校验可信租户及候选人、申请、职位、
  阶段事件、自然人、劳动关系、员工、离职案件和校友授权引用闭包，错位数据整体
  失败关闭。10 项专项测试与四个生产文件达到四维 100%，新增逐文件四维 90%
  门禁；REST 和标准 MCP 契约不变，MCP 仍只读最小投影。真实全周期数据回放、
  部门权限映射和跨角色 UAT 仍待现场验收。
- 2026-07-29 已加固 Talent Lifecycle 写入口与事务 Outbox：REST 对资源 ULID、
  幂等键、强 `If-Match`、严格请求体和未知字段失败关闭；业务失败记录独立 R2
  失败审计，事务提交后的成功审计故障只告警，不把已提交终态回写为失败。
  Outbox 对创建、完成和取消三类触点执行逐字段白名单、动作/状态/版本组合、
  可信租户、活动 Mongo 事务、CloudEvent 最小负载及数据库回执反向绑定。
  80 项专项测试使两个生产文件四维均达到 100%，新增两个逐文件四维 90% 门禁；
  REST、事件名与标准 MCP 契约不变，MCP 仍不开放触点写入、Outbox 或重放。
  真实事件总线、角色映射和跨角色 UAT 仍待现场验收。
- 2026-07-28 已加固 Recruitment → Onboarding 跨域桥：预入职前复核接受证据
  与 Offer/Application/Candidate/Position 完整引用闭包，录用终态只接受具备
  签署证据的已签 Offer；`hired` 阶段事件绑定 Onboarding 完成证据，
  Candidate Application 单独保存 Employment 引用。31 项桥接专项测试和目标
  生产文件四维覆盖率均为 100%，独立 90% 门禁已接入 `pnpm check`；标准 MCP
  不新增该写能力，真实 eSign、全链路回放与 HR UAT 仍待现场验收。
- 2026-07-28 已加固 Care → Org 关怀来源窄口：只接受同租户服务主体或系统任务，
  并闭合校验 Employee 查询主键、当前 Employment、Person、唯一开放关系、全部
  复聘历史及生日证明/登记时间/盲索引三元组。损坏引用不再降级为正常不具备资格，
  避免静默取消待发任务。43 项专项测试使目标生产文件达到
  98.52%/98.83%/100%/100%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`；标准 MCP 仍只读取 Care 应用服务的本人脱敏汇总，不直连 L4
  来源窄口或新增写能力。
- 2026-07-29 已加固 Care 关怀通知网关边界：外呼前严格校验完整请求、唯一偏好
  渠道、独立可见 ASCII 凭据、规范 Key ID 与 Ed25519 SPKI DER 公钥；请求使用
  固定路径、无压缩响应和最小控制字段。回执执行严格 JSON Content-Type、规范
  Content-Length、16 KiB 流式上限、异常取消、Fatal UTF-8、规范 Base64/
  Base64URL、原始字节验签、上下文/渠道/时间闭包和稳定错误码。25 项专项测试使
  目标生产文件达到 95.59%/94.44%/92.85%/96.66%（语句/分支/函数/行），
  逐文件四维 90% 门禁已接入 `pnpm precheck`；REST、事件和标准 MCP 不新增
  通知发送、渠道解析、联系方式或证据读取能力。
- 2026-07-29 已加固 Care 事务 Outbox 发布边界：离职、校友授权、授权终止清理
  与生日/周年关怀共 22 类事件采用完整对象和逐类型负载白名单，严格绑定状态、
  版本、规范时间、可信租户/主体和活动 Mongo 事务；个人、联系方式、授权证据、
  清理证明、通知正文、Token 及未知字段在落库前失败关闭。创建回执须反向绑定
  ULID、聚合、完整 CloudEvent 与调度时间。60 项专项测试达到
  98.71%/91.89%/100%/100%（语句/分支/函数/行），独立四维 90% 门禁已接入
  `pnpm precheck` 与 `pnpm check`；事件名、REST 与标准 MCP 契约不变，MCP
  仍不开放发送、重放、证明或联系方式能力。
- 2026-07-29 已加固 Onboarding 事务 Outbox 发布边界：创建、任务完成、建档
  开始与完成四类事件严格绑定状态、任务代码、Employment 引用、规范时间、可信
  租户/主体和活动 Mongo 事务；未知字段、敏感证据、Token、跨租户事件及分裂
  上下文在落库前失败关闭，创建回执须反向绑定事件 ULID、聚合版本、完整
  CloudEvent 与调度时间。43 项专项测试达到
  98.27%/96.87%/100%/100%（语句/分支/函数/行），逐文件四维 90% 门禁
  已接入 `pnpm precheck` 与 `pnpm check`；REST、事件名和标准 MCP 契约
  不变，MCP 仍只经入职应用服务读取部门裁剪摘要。
- 2026-07-29 已加固 Onboarding 聚合与任务证明仓储边界：固定最小投影逐条
  反向绑定可信租户、实例/Offer/候选人查询主键、状态、版本、规范时间和任务/
  完成/Employment 证据闭包；候选人时间线强制一百条上限、稳定顺序和唯一标识。
  聚合插入、乐观锁更新及证明追加要求活动 Mongo 事务，数据库创建与更新回执
  必须反向绑定全部不可变引用和目标终态。104 项专项测试使目标文件四维达到
  100%，独立四维 90% 门禁已接入 `pnpm precheck` 与 `pnpm check`；REST、
  事件和标准 MCP 契约不变，MCP 仍不读取证明或执行建档。
- 2026-07-29 已加固 Recruitment 事务 Outbox 发布边界：申请、候选人、HC、
  职位、面试、Offer 与简历分析共 31 类事件逐类型绑定聚合、状态、审批/投递/
  接受/eSign 证据链、规范时间和活动 Mongo 事务；未知字段、L3/L4 原文、Token、
  跨租户事件及分裂上下文在落库前失败关闭，创建回执须反向绑定事件 ULID、
  聚合版本、完整 CloudEvent 与调度时间。83 项专项测试达到
  99.29%/98.43%/100%/100%（语句/分支/函数/行），逐文件四维 90% 门禁
  已接入 `pnpm precheck` 与 `pnpm check`；REST、事件名和标准 MCP 契约
  不变，MCP 仍经招聘应用服务提供授权裁剪能力。
- 2026-07-29 已加固 Recruitment Offer REST、DTO 与领域入口：资源 ULID、
  强 ETag、8–128 位白名单幂等键和精确正文在副作用前失败关闭，无正文动作只
  允许缺失正文或严格空普通对象。L4 条款由 DTO 与领域双重校验精确形状、规范毫秒级 UTC、
  真实日历日期、安全版本和单调时间；业务失败审计异常保留原错误，事务提交后的
  成功审计异常只告警。165 项入口测试使三个生产文件四维均为 100%；27 项应用
  服务测试达到 95.97%/94.05%/100%/96.84%，新增逐文件四维 90% 门禁并接入
  `pnpm precheck`、`pnpm check` 与全局覆盖链。REST、事件和标准 MCP 契约
  不变；真实 Approval、eSign、招聘渠道和 HR UAT 仍待现场验收。
- 2026-07-28 已加固钉钉/飞书考勤 Provider 外部响应闭包：请求租户、唯一员工
  集合、日期和时区必须规范；返回任务员工、嵌套记录员工、事件 ID 与事实本地
  日期逐项反向绑定原请求，重复任务/事件、记录 ID 冲突、窗口外事实和证据时间线
  漂移整批失败关闭。Normalizer/EvidenceVerifier 提升到 v2，令牌仍只允许一次
  401 刷新。49 项专项测试使目标生产文件达到
  95.21%/92.02%/100%/95.42%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`；标准 MCP 不新增 Provider 拉取、Inbox、凭据、重放或处置能力。
  租户上下文、生产运行入口与可观测性边界、审计追加与 WORM 运输、审计后台执行与队列观测、审计链锚定、审计链验证、组织主数据应用、组织主数据入口、身份授权、
  审批数据加密、审批仓储、审批应用状态机、审批主体解析、审批入口控制器、审批模板领域、审批 Outbox 运行时边界、MCP 确认服务、MCP HTTP 入口、MCP 运行时、MCP Tool 应用层、OP 审批桥入站申请、OP 审批桥可信读取、OP 审批结果运维、OP 审批结果回传、OP Webhook 双入口、OP 经营摘要处理、薪酬影子周期、薪酬运行、
  薪酬审批、薪酬主数据、专业算薪主数据快照、薪酬四方对账、薪酬税务申报、薪酬 L4 数据加密、资金支付、Treasury WORM 证据出口、Treasury 银行提交出站边界、Treasury 银行回盘、Treasury 银行回盘入站边界、Treasury L4 数据加密、Treasury Outbox、Phase 4 REST 入口、Care 纪念日应用、Care 通知网关信任边界、Care Outbox 运行时边界、Care 离职应用、数据迁移控制面、数据迁移打包 CLI、Knowledge 考试运行 Relay、
  Knowledge 搜索索引 Relay、Knowledge 考试持久化与 Outbox 边界、Knowledge 评分证据与搜索网关边界、Knowledge REST 入口控制器、Knowledge 应用服务、Knowledge 考试应用与入口、Knowledge 领域模型、Knowledge 持久化 Schema、Knowledge 考试重放 CLI、考勤应用、考勤仓储、考勤供应商拉取、考勤 Provider 外部响应闭包、考勤供应商入站处理、电子签回调处理、电子签发起状态机、招聘渠道拉取、招聘渠道入站处理、招聘渠道职位扇出、招聘渠道阶段扇出、招聘申请、招聘面试、招聘简历、招聘渠道职位投递、招聘渠道阶段回传、招聘渠道人工处置、招聘管理、人才全周期应用、人才全周期仓储、招聘 Offer、Care 仓储、组织仓储、招聘仓储、
  知识库仓储、营销 CMS、营销入口与幂等核心、营销副作用可靠投递、审批通知可靠投递、审批通知运维边界、组织主数据外部投递可靠性、组织外部身份解析边界、组织平台适配器安全边界、组织首次平台开户、身份令牌与 OAuth 授权事务、身份用户会话与签名键轮换、人员 SSO 信任边界、OAuth Client Credentials 服务身份签发、OAuth 授权控制器、WebAuthn 强认证、入职应用与入口控制器、
  校友授权清理协调、校友授权清理证明出口、Attendance 规则纵切、Attendance 核心领域与 L4 加密、生产执行授权服务、Phase 5 管理分析、Payroll Tax 双出口、Treasury WORM 证据出口、招聘面试日历可靠投递、数据迁移控制面入口与附件链路、人才全周期四域来源完整性、招聘入职跨域桥、关怀跨域来源、自然人生日证明入口、Knowledge 搜索索引事务任务写入、Onboarding 仓储运行时边界、Onboarding Outbox 运行时边界、Recruitment Outbox 运行时边界、Recruitment Offer REST/DTO/领域入口、人才全周期 REST 入口、人才全周期 Outbox 运行时边界与本人薪资单迁移边界已建立
  90% 门禁，当前覆盖率基线集合为
  100%/100%/100%/100%、100%/100%/100%/100%、100%/100%/100%/100%、
  100%/100%/100%/100%、
  97.44%/93.52%/100%/97.50%、
  100%/100%/100%/100%、
  100%/95.83%/100%/100%、
  98.75%/96.96%/100%/100%、
  98.06%/94.04%/98.64%/99.02%、
  94.62%/90.76%/98.19%/95.96%、100%/100%/100%/100%、
  97.55%/96.32%/100%/97.60%、
  100%/97.74%/100%/100%、
  100%/100%/100%/100%、
  97.97%/95.67%/97.32%/98.88%、
  100%/100%/100%/100%、
  100%/100%/100%/100%、
  100%/100%/100%/100%、
  98.34%/96.13%/100%/100%、
  99.34%/97.35%/100%/100%、
  99.00%/97.24%/97.29%/98.84%、92.18%/90.20%/96.10%/93.18%、
  100%/100%/100%/100%、
  100%/100%/100%/100%、
  98.21%/96.42%/100%/100%、
  96.42%/96.79%/100%/98.96%、
  98.88%/98.58%/100%/100%、
  96.51%/94.54%/100%/98.59%、
  93.88%/93.23%/96.05%/95.77%、
  97.04%/96.27%/100%/97.05%、
  98.26%/96.82%/100%/99.00%、
  98.48%/98.67%/100%/98.46%、
  100%/100%/100%/100%、
  100%/96.85%/100%/100%、
  99.54%/97.43%/100%/100%、
  100%/99.35%/100%/100%、
  93.37%/90.19%/97.97%/95.29%、
  95.26%/91.55%/92.30%/95.69%、
  98.06%/94.04%/100%/100%、
  98.52%/98.83%/100%/100%、
  100%/100%/100%/100%、
  100%/100%/100%/100%、
  99.59%/94.85%/98.21%/99.53%、
  100%/98.85%/100%/100%、
  99.35%/100%/95.45%/100%、
  100%/98.85%/100%/100%、
  95.93%/94.78%/93.75%/97.27%、
  99.20%/99.17%/100%/99.11%、
  100%/100%/100%/100%、
  98.61%/97.00%/100%/99.21%、
  100%/100%/100%/100%、
  100%/100%/100%/100%、
  98.22%/95.47%/100%/98.40%、
  100%/100%/100%/100%、
  100%/100%/100%/100%、
  100%/100%/100%/100%、
  100%/99.35%/100%/100%、
  100%/100%/100%/100%、
  99.30%/90.78%/100%/100%、
  95.45%/92.38%/100%/97.25%、
  96.52%/92.36%/100%/97.72%、
  100%/94.11%/100%/100%、
  99.51%/99.45%/100%/100%、
  100%/96.85%/100%/100%、
  100%/97.05%/100%/100%、
  95.97%/94.01%/100%/96.83%、
  100%/91.66%/100%/100%、100%/100%/100%/100%、
  97.44%/91.32%/100%/99.53%、100%/99.43%/100%/100%、
  100%/97.62%/100%/100%、
  99.70%/96.85%/100%/99.68%、
  99.13%/98.33%/100%/99.51%、
  100%/98.30%/100%/100%、
  96.09%/91.51%/100%/99.45%、
  100%/100%/100%/100%、
  100%/98.23%/100%/100% 和
  100%/100%/100%/100%、98.27%/97.70%/96.55%/98.34%、
  97.18%/95.18%/100%/97.99%、99.02%/97.69%/100%/100%、
  98.61%/95.04%/100%/99.07%、
  98.23%/97.43%/98.24%/98.85%、97.80%/93.10%/100%/100%、
  99.59%/98.23%/100%/99.56%、97.60%/95.14%/100%/99.07%、
  99.17%/95.97%/100%/99.13%、96.96%/95.60%/100%/98.79%、
  99.70%/94.44%/100%/99.67%、99.13%/98.03%/100%/99.02%、
  99.01%/96.70%/100%/98.87%、98.90%/96.15%/100%/98.70%、
  99.35%/98.57%/100%/99.25%、100%/97.77%/100%/100%、
  96.39%/96.15%/96.42%/97.59%、100%/100%/100%/100%、
  95.21%/92.02%/100%/95.42%、92.17%/91.90%/98.00%/93.33%、
  95.59%/94.44%/92.85%/96.66%、100%/100%/100%/100%、
  100%/100%/100%/100%、100%/100%/100%/100%、
  98.71%/91.89%/100%/100%、95.71%/94.06%/100%/97.42%、
  100%/98.24%/100%/100%、98.27%/96.87%/100%/100%、
  99.29%/98.43%/100%/100%、100%/100%/100%/100%、
  100%/100%/100%/100%、100%/100%/100%/100%
  （语句/分支/函数/行），并接入 `pnpm check`。后续轮次必须继续补齐全仓及
  其余关键链路，禁止以排除生产文件或降低阈值宣称完成。
- 2026-07-28 已加固审计追加与独立 WORM 运输：链载荷必须是严格、规范 JSON，
  HMAC 哈希拒绝非规范 Base64URL；WORM 请求绑定载荷 SHA-256、Ed25519 签名与
  保留期，端点、凭据、内容类型、体积、UTF-8、回执时间和保留期全部失败关闭；
  Mongo 事务提交后的会话清理故障不再诱发重复审计追加。三个生产文件已建立逐文件
  四维 90% 门禁，真实独立权限域 WORM 回读与验签仍待现场验收。
- 2026-07-28 已补齐审计后台执行与队列观测：锚定 Processor 只接受固定任务名和
  严格空载荷，拒绝客户端租户或资源上下文；WORM 未启用时不注册空转任务，损坏
  配置或调度注册失败时 Worker 启动失败关闭；启用后以固定六小时调度、100 租户
  批量、五次指数退避执行。队列指标立即及每十五秒采集且禁止并发刷新，API 模块
  不装配 Processor/Scheduler。17 项专项测试覆盖七个生产文件，逐文件四维均为
  100%，独立门禁已接入 `pnpm precheck`。
- 数据迁移控制面已有 61 项幂等、证据分页、检查点竞争、关联、附件与全域负载
  失败关闭测试，服务覆盖率达到 93.37%/90.19%/97.97%/95.29%
  （语句/分支/函数/行），已建立独立 90% 门禁并接入 `pnpm check`。
- 2026-07-29 已加固业务附件迁移边界：迁移输入和 WORM 激活回执采用严格运行时
  契约；持久化读取只取最小投影，并反向校验可信租户、迁移引用、归属类型/用途、
  checksum、状态、版本、对象证据和可用时间组合。受损记录在事务更新与 Outbox
  发布前失败关闭；available 重放仅接受同一目标证据。37 项服务测试达到
  100%/98.18%/100%/100%（语句/分支/函数/行）。既有数据迁移入口与附件链路
  门禁扩展为 169 项、九个生产文件，合计达到
  99.54%/98.24%/100%/99.50%，逐文件四维 90% 门禁继续接入
  `pnpm precheck` 与 `pnpm check`；标准 MCP 仍不暴露附件对象、checksum、
  上传员工或迁移写能力。
- 数据迁移打包 CLI 已覆盖 19 项来源包流式校验、符号链接逃逸、断点续传、附件
  等待、远端响应、证据分页与控制总数测试；负数待传数、重复游标、超大页面、
  非白名单远端错误码及含凭据/query/fragment 的端点均失败关闭。覆盖率达到
  95.26%/91.55%/92.30%/95.69%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- Knowledge 考试运行 Relay 已补齐 24 项隔离网关、超时封存、自动/人工评分、
  事务终态、审计隔离、退避、死信与熔断测试，服务覆盖率达到
  98.06%/94.04%/100%/100%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- Knowledge 搜索索引 Relay 已补齐 5 项幂等回执、时间边界、参数约束、认领竞争、
  指数退避与死信测试，服务覆盖率达到四维 100%，独立 90% 门禁已接入
  `pnpm check`。
- Knowledge 评分证据与搜索网关信任边界已覆盖 20 项严格请求、人工复核证据
  绑定、独立 Origin/凭据/Ed25519 公钥/Key ID、规范 Base64、Content-Type、
  Content-Length、流取消、Fatal UTF-8 和授权检索测试；答案、Token、未知字段
  及人工复核策略错位均在外呼前失败关闭。目标生产文件达到
  92.17%/91.90%/98.00%/93.33%（语句/分支/函数/行），逐文件 90% 门禁已
  接入 `pnpm precheck`；REST、事件与标准 MCP 不新增评分或索引写能力。
- Knowledge REST 入口控制器已覆盖 9 项完整路由、精确 Scope、ULID、强
  `If-Match`、幂等键、ETag、最小审计元数据与提交后审计隔离测试，覆盖率达到
  四维 100%，独立 90% 门禁已接入 `pnpm check`。
- Knowledge 应用服务已覆盖 41 项课程创建/发布/下架、任务分配/读取、员工主数据
  解析、全文检索授权与索引新鲜度、集成进度幂等、任务完成和入职证明终态测试；
  发布校验只在幂等事务的新执行分支内执行；Mongo 自动重试相同课程快照时复用
  校验结果，重试读取到不同版本或内容引用时必须重新校验并拒绝旧版本写入。
  覆盖率达到 99.59%/94.85%/98.21%/99.53%（语句/分支/函数/行），独立
  90% 门禁已接入
  `pnpm check`。
- Knowledge 考试应用与入口已覆盖 28 项所有权、活动运行、尝试上限、完整考试
  策略、提交状态与截止时间、唯一键竞争、最小响应和提交后审计隔离测试；考试
  开始与提交后的审计失败只记录稳定告警，R0 读取审计仍失败关闭。两个目标文件
  合计覆盖率达到 100%/98.85%/100%/100%（语句/分支/函数/行），且各自均已
  接入独立 90% 门禁。
- Knowledge 领域模型已覆盖 39 项课程、受众、任务、考试策略、评分、版本、租户、
  时间和终态不变量；主观题与混合题缺少人工复核证据时在领域构造阶段即失败关闭，
  自动评分测试事实固定为客观题。两个目标文件合计覆盖率达到
  99.35%/100%/95.45%/100%（语句/分支/函数/行），且各自均已接入独立 90%
  门禁。
- Knowledge 持久化 Schema 已覆盖 22 项课程授权、可空考试配置、培训任务状态与
  证据、考试运行状态机、超时、锁、受控重放及搜索索引证据组合测试；无考试课程
  允许 `passingScoreBps=null`，受损任务记录在 Mongo 校验边界失败关闭。三个目标文件
  合计覆盖率达到 100%/98.85%/100%/100%（语句/分支/函数/行），且各自均已
  接入独立 90% 门禁。
- Knowledge 考试重放 CLI 已覆盖 40 项证据状态推断、精确参数、重复参数、
  十进制版本、数据库配置、连接关闭、稳定错误码、事务竞争、脱敏 Outbox 和
  受损 dead 记录失败关闭测试；重放前会验证引用链、题型、次数和超时事实。
  覆盖率达到 95.93%/94.78%/93.75%/97.27%（语句/分支/函数/行），独立
  90% 门禁已接入 `pnpm check`。
- 考勤仓储已覆盖 17 项可信租户、源事实/修订/月快照密文读写、盲索引、迁移证据、
  会话绑定、并发冲突和失败关闭测试，目标生产文件达到四维 100%，独立 90%
  门禁已接入 `pnpm check`。
- Attendance 核心领域与 L4 加密已覆盖 99 项事实/修订/月结不变量、严格日历时间、
  规则日摘要、迁移时间线、密钥轮换、盲索引命名空间、AAD、认证篡改和解码前
  长度上限测试；两个生产文件合计达到 98.23%/97.43%/98.24%/98.85%
  （语句/分支/函数/行），且各自四维均不低于 90%。独立门禁已接入
  `pnpm precheck`。
- Attendance 规则纵切已覆盖 85 项规则/排班构造、`Employment` 有效区间、
  Provider 覆盖对账、跨天归属、REST、仓储、Schema、Outbox 最终事件名和失败
  关闭测试；八个目标文件合计达到
  98.61%/95.04%/100%/99.07%（语句/分支/函数/行），且每个文件四维均不低于
  90%。独立门禁已接入 `pnpm precheck`，全量覆盖门禁已接入 `pnpm check`。
- 考勤供应商拉取已覆盖 50 项系统任务授权、加密游标、员工分页、小批拉取、Inbox
  幂等、租约竞争、载荷规范化和失败关闭测试；部分游标密文字段不再被误判为首次
  同步。覆盖率达到 98.61%/97.00%/100%/99.21%（语句/分支/函数/行），独立
  90% 门禁已接入 `pnpm check`。
- 考勤供应商入站处理已覆盖 30 项任务分派、可信系统身份、加密信封、传输证据、
  标准化器版本、员工盲索引映射、检查点与租约竞争测试；业务成功、人工复核及
  失败终态后的审计故障均只作独立告警。覆盖率达到四维 100%，独立 90% 门禁
  已接入 `pnpm check`。
- 电子签纵切已覆盖 298 项 V3 创建流程、官方免登录签署链接、请求签名、HTTPS
  边界、加密信封、状态投影、证据扫描/WORM、重复与乱序回调、Webhook 三元租约
  fencing、失败任务重建、审计故障隔离和唯一键竞态测试。状态投影另补齐严格
  当前状态校验、重复未知状态/终态冲突幂等、终态供应商状态保留和人工复核原因
  保持。十个核心文件聚合覆盖率达到
  98.11%/95.70%/99.04%/99.54%（语句/分支/函数/行），各文件四维均
  不低于 90%；`pnpm quality:esign-integration-coverage` 已接入 `pnpm check`。
  真实 eSign 租户、实名认证、免登签署、拒签/过期/撤销回调和 WORM 网关仍须
  现场联调，不以本地契约测试替代。
- 招聘渠道补拉边界已覆盖 85 项 Registry 装配、系统任务授权、Mongo 绑定回读
  闭包、加密游标前进、精确响应、事件唯一性、纯 JSON 资源预算、Inbox 幂等和
  租约失败关闭测试；两个生产文件合计达到
  98.22%/95.47%/100%/98.40%（语句/分支/函数/行），逐文件四维均不低于
  90%，既有独立门禁已扩展并接入 `pnpm check`。
- 招聘渠道入站处理已覆盖 47 项任务分派、可信系统身份、标准化与证据检查点、
  加密外部映射、唯一键竞态、回执盲指纹、租约竞争和失败关闭测试；业务成功、
  人工复核及失败终态后的审计故障均只作独立告警，不再覆盖已提交终态或原始异常。
  覆盖率达到四维 100%，独立 90% 门禁已接入 `pnpm check`。
- 招聘渠道职位扇出已覆盖 22 项 Worker 参数、过期认领、草稿创建、开放/暂停/
  关闭状态、活动渠道幂等扇出、事务认领竞争、退避、死信和释放租约测试；失败
  释放必须仍持有原 Worker 租约。覆盖率达到四维 100%，独立 90% 门禁已接入
  `pnpm check`。
- 招聘渠道阶段扇出已覆盖 28 项 Worker 参数、过期认领、创建事件跳过、内部阶段
  脱敏映射、事务认领竞争、退避、死信和释放租约测试；失败释放必须仍持有原
  Worker 租约。覆盖率达到四维 100%，独立 90% 门禁已接入 `pnpm check`。
- 招聘申请已覆盖 22 项候选人与申请迁移、WORM 证据不可变重放、职位与授权引用、
  渠道可信入口、部门级阶段写范围、乐观锁和稳定错误契约测试；阶段推进在事务内
  重新读取职位并强制部门写范围，只有 `erp:recruitment:management:write_all`
  可跨部门。覆盖率达到 100%/99.35%/100%/100%（语句/分支/函数/行），独立
  90% 门禁已接入 `pnpm check`。
- 招聘面试已覆盖 19 项迁移 WORM 证据不可变重放、员工与申请引用、部门级写范围、
  面试官身份映射、评价完整性、终态、日历敏感投影、乐观锁和稳定错误契约测试；
  日历投影除专用 Scope 外还必须来自 `system_job`，普通用户不能借同名 Scope
  读取 L3 地点和面试官列表。覆盖率达到四维 100%，独立 90% 门禁已接入
  `pnpm check`。
- 招聘简历已覆盖 10 项可信附件入口、租户内读写、受控标签、人工复核、Worker
  租约、非法 AI 标签和失败终态测试；分析正文入口除专用 Scope 外还必须来自
  `system_job`，防止普通用户触发敏感简历解析。覆盖率达到
  99.30%/90.78%/100%/100%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- 审批模板领域已覆盖 20 项定义/表单白名单、审批人解析器、职责分离、发布/修订/
  退役、迁移生命周期、快照完整性与规范化哈希测试；在线及迁移快照会再次拒绝
  状态与 `retiredAt` 不一致的受损聚合。覆盖率达到
  97.55%/96.32%/100%/97.60%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- 审批入口控制器已覆盖 41 项完整 REST、精确 Scope、ULID、幂等键、强
  `If-Match`、ETag、模板/委托/实例操作与审计终态测试；R0 读取审计保持失败
  关闭，R1/R2 写入提交后的审计故障只作稳定告警，不反向暴露为业务失败。
  覆盖率达到四维 100%，独立 90% 门禁已接入 `pnpm check`。
- 招聘渠道职位投递已覆盖 40 项强版本顺序、稳定幂等键、加密外部映射、唯一键
  竞态、发布/暂停/下架、精确回执、运行时租户闭包、结果未知隔离和显式
  `not_committed` 重试测试；业务终态后的审计故障只作独立告警。覆盖率达到
  95.45%/92.38%/100%/97.25%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- 招聘渠道阶段回传已覆盖 30 项强版本顺序、本地来源跳过、加密申请映射、稳定
  幂等键、精确回执、运行时租户闭包、结果未知隔离和显式 `not_committed`
  重试测试；业务终态后的审计故障只作独立告警。覆盖率达到
  96.52%/92.36%/100%/97.72%
  （语句/分支/函数/行），独立 90% 门禁已接入 `pnpm check`。
- 招聘渠道人工处置已覆盖 19 项可信租户分页、脱敏摘要、职位/阶段统一处置、
  幂等重入、批准例外、供应商确认未提交、R2 审计和非法参数测试；不能伪造
  外部成功，且明确不注册 MCP Tool。两个生产文件合计覆盖率达到
  100%/94.11%/100%/100%，独立 90% 门禁已接入 `pnpm check`。
- 招聘渠道补拉边界已覆盖 85 项凭据命名空间、到期调度、可信系统身份、绑定回读
  闭包、加密游标、精确投递协议、JSON 资源预算、盲索引去重、唯一键竞态、确定性
  队列恢复和失败租约测试；两个生产文件合计达到
  98.22%/95.47%/100%/98.40%（语句/分支/函数/行），逐文件四维均不低于
  90%，独立门禁已接入 `pnpm check`。
- 招聘管理应用服务已覆盖 70 项可信租户、部门级读写范围、HC 审批 Saga 恢复、
  迁移审批证据、不可变重放、组织引用、职位状态机、门户最小投影、乐观锁和错误
  映射测试；提交与审批同步在预检和事务内均强制部门写范围，已绑定审批的待审批
  HC 按精确版本幂等恢复。服务覆盖率达到 99.51%/99.45%/100%/100%
  （语句/分支/函数/行）。另有 97 项 REST/DTO/领域入口测试覆盖严格 ULID、
  幂等键、强 ETag、精确空正文、运行时字段与状态、版本上溢、时间倒退及提交后
  审计隔离；目标文件逐文件四维均不低于 90%，独立门禁
  `pnpm quality:recruitment-management-entry-coverage` 已接入 `pnpm precheck`、
  `pnpm check` 与全局覆盖链。
- 人才全周期应用已覆盖 17 项读写 Scope、最小 MCP 投影、全阶段推导、完整时间线、
  候选人/校友联系授权、触点负责人、乐观锁与稳定错误契约测试；候选人联系授权
  或保留期为非法时间时改为失败关闭，避免 `Date.parse()` 的 `NaN` 绕过。覆盖率
  达到 100%/96.85%/100%/100%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- 人才全周期仓储已覆盖 20 项可信租户、授权前非敏感投影、备注密文组合、AAD
  解密、候选人时间线、会话绑定、跨租户拒绝、乐观锁和损坏密文失败关闭测试；
  覆盖率达到 100%/97.05%/100%/100%（语句/分支/函数/行），独立 90%
  门禁已接入 `pnpm check`。
- 人才全周期 REST 入口与事务 Outbox 已覆盖 80 项最小 Scope、严格 ULID、
  幂等键、强版本条件、请求白名单、业务失败审计、提交后审计隔离、可信租户、
  活动事务、三类触点动作/状态/版本闭包、最小 CloudEvent 和数据库回执反向绑定
  测试；两个生产文件四维均达到 100%，两个独立 90% 门禁已接入
  `pnpm precheck` 与 `pnpm check`。事件不含姓名、联系方式、备注、负责人或
  方向；标准 MCP 继续只复用应用服务的只读最小投影。
- 身份令牌与 OAuth 授权事务已覆盖 39 项 JOSE 验签、主体绑定、授权集合唯一性、
  人员会话、MCP 凭据即时撤销、当前客户端回调/resource/租户/Scope 重验、PKCE、
  随机碰撞、一次性原子消费和受损 Redis 记录测试。进程重启或配置收紧后旧请求
  与授权码不再沿用旧授权；两个目标文件合计达到
  97.80%/93.10%/100%/100%（语句/分支/函数/行），且各自四维均不低于 90%。
  独立门禁已接入 `pnpm precheck`。
- OAuth 授权控制器已覆盖 45 项预注册回调、PKCE、授权决策、授权码、
  `client_credentials`、协议错误与限流测试；Basic 和 `private_key_jwt`
  的限流主体只取自认证材料，正文 `client_id` 不能轮换限流桶；决策提交后的
  审计故障只作独立告警，不再改写业务终态。覆盖率达到
  96.09%/91.51%/100%/99.45%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- OAuth Client Credentials 服务身份签发已覆盖 22 项规范 Basic、严格 UTF-8、
  官方 MCP SDK `private_key_jwt`、短时断言、`jti` 原子防重放、resource/Scope
  越权和故障审计测试。正文 `client_id` 不再能注入失败审计归属；认证后的资源
  与 Scope 拒绝在令牌签名前分别记录稳定 R1 失败审计。覆盖率达到
  97.60%/95.14%/100%/99.07%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm precheck`。
- 身份用户会话与签名键轮换已覆盖 11 个测试文件、82 项测试。Refresh Token
  family 轮换增加原子前驱消费、后继 CAS、重放后整族/会话吊销、单一 Cookie 与
  8 KiB Header 边界、受损持久化状态失败关闭；JWKS 同时发布活动键和最多五把
  历史验签键，拒绝私钥、弱 RSA、重复 `kid`/公钥材料并支持两阶段轮换。会话
  刷新签名在轮换事务提交前完成，签名失败会回滚令牌代次；吊销提交后的审计异常
  不再改写已提交终态。十二个生产文件合计达到
  99.17%/95.97%/100%/99.13%（语句/分支/函数/行），逐文件四维 90% 门禁已
  接入 `pnpm precheck`。
- 人员 SSO 信任边界已覆盖 13 个测试文件、136 项测试。钉钉、飞书、OP 回调逐层
  绑定 state、平台、外部租户与 ERP 租户，固定上游 HTTP 拒绝重定向、任意地址
  和超限响应；租户绑定、外部身份双标识映射与授权快照均从最小投影转换为经运行时
  完整性校验的冻结对象，跨租户或受损记录在令牌签发前失败关闭；浏览器 state
  Cookie 另拒绝超长 Header、重复同名值、异常摘要与跨 Origin 回调，并在所有回调
  结果后清除。十五个生产文件合计达到 99.58%/99.20%/100%/100%
  （语句/分支/函数/行），逐文件四维 90%
  门禁已接入 `pnpm precheck`；真实企业租户权限、平台限流与端到端 UAT 待外部验收。
- WebAuthn 强认证已覆盖 38 项可信会话、登记权限、RP/Origin/UV、凭据归属、
  仪式一次性消费、计数器竞争、证据绑定与审计故障测试；登记完成阶段会再次校验
  Passkey 管理 Scope，权限撤销后不能用旧仪式落库新凭据，登记或撤销提交后的
  审计故障只作独立告警。服务和入口控制器均达到四维 100%，独立 90% 门禁已
  接入 `pnpm check`。
- 入职应用与入口控制器已覆盖 67 项可信租户、Scope、部门数据范围、幂等重放、
  招聘来源一致性、任务证据、强 `If-Match`、R3 建档 Saga 和审计终态测试；
  读取、组织分配与合同同步均在外部调用前二次授权，已完成 Saga 只接受当前至
  前两版的恢复窗口，业务提交后的审计故障只作独立告警。合并覆盖率达到
  100%/98.23%/100%/100%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- Onboarding Outbox 运行时边界已覆盖 43 项四类事件负载、可信租户/主体、活动
  事务、规范时间、状态组合、并发输入篡改、数据库异常和完整创建回执反向绑定
  测试。目标 Writer 达到 98.27%/96.87%/100%/100%
  （语句/分支/函数/行），独立四维 90% 门禁已接入 `pnpm precheck` 与
  `pnpm check`；标准 MCP 仍只读且不直连 Outbox。
- Recruitment Outbox 运行时边界已覆盖 83 项 31 类事件负载、聚合/状态、
  审批与外部证据引用、可信租户/主体、活动事务、调用方并发篡改、数据库异常和
  完整创建回执反向绑定测试。目标 Writer 达到
  99.29%/98.43%/100%/100%（语句/分支/函数/行），独立四维 90% 门禁
  已接入 `pnpm precheck` 与 `pnpm check`；标准 MCP 不直连 Outbox。
- Care 离职应用已补齐 36 项可信组织主数据、审批恢复、清算证据、R3 Saga、
  校友授权和异常语义测试，服务覆盖率达到 99.54%/97.43%/100%/100%
  （语句/分支/函数/行），独立 90% 门禁已接入 `pnpm check`。
- 校友授权清理协调与执行服务已覆盖 67 项可信系统任务、事件信封与源状态绑定、
  目标政策幂等、事务回调、认领竞争、退避/死信、队列恢复、证明终态和最小 MCP
  摘要测试；畸形终止事件会立即释放认领并计入重试，事务回调未执行不得误报成功。
  两个目标文件合计覆盖率达到 100%/99.35%/100%/100%（语句/分支/函数/行），
  独立 90% 门禁已接入 `pnpm check`。
- MCP HTTP 入口已覆盖 26 项固定路由、精确 Origin、尾斜杠、连接 Scope、HttpOnly 会话、
  R1/R2 状态、WebAuthn 仪式、显式可信用户审计和审计故障测试。公共确认端点不再
  错用缺失的请求租户上下文；确认状态推进后的审计异常只记录稳定告警，不把已生成
  的一次性凭据反向暴露为失败。确认控制器、全局 Origin Guard 和 MCP 控制器均达到
  四维 100%，独立 90% 门禁已接入 `pnpm check`。
- 标准 MCP 运行时已使用官方 Client 覆盖 25 个 Prompt、50 个 Tool 与 31 个
  受控 Resource 入口，并验证 Origin 白名单、参数失败关闭、最小 fallback 和
  无权读取语义；覆盖率达到 97.97%/95.67%/97.32%/98.88%
  （语句/分支/函数/行），独立 90% 门禁已接入 `pnpm check`。
- MCP Tool 应用层已覆盖 43 项可信身份、Scope、只读投影、R1/R2 准备与执行、
  确认重放、命令错配、非法 Resource Link、服务端幂等键和提交后审计隔离测试；
  返回值在确认账本完成前校验并序列化，账本完成后的审计故障只形成稳定告警，
  不释放已完成操作。覆盖率达到四维 100%，独立 90% 门禁已接入 `pnpm check`。
- OP 审批桥入站申请已覆盖 32 项可信 Worker 上下文、载荷完整性、受控路由、
  来源单据唯一桥接、租约、4xx/5xx 分类、重放、桥接冲突和提交后审计隔离测试；
  审批与 Inbox 终态提交后的审计故障只形成稳定错误，不回写失败或覆盖原始错误，
  外部异常码仅接受固定格式。覆盖率达到四维 100%，独立 90% 门禁已接入
  `pnpm check`。
- 2026-07-29 已加固 OP 审批桥可信读取边界：REST 与标准 MCP 共用应用服务
  二次校验 Scope，仅按可信租户和规范 eventId 查询固定最小投影，并反向绑定
  租户、eventId、标识格式、状态、版本和完成/更新时间关系；未知字段、错绑及
  非法状态组合整体失败关闭。终态 Relay 在禁用自动时间戳时同步写入完成与更新
  时间。35 项读取测试达到四维 100%，独立 90% 门禁已接入 `pnpm precheck`
  与 `pnpm check`；MCP 仍只读且不直连数据库、不返回表单或连接凭据。
- 2026-07-29 已加固 OP 审批结果运维边界：查询和人工重试在应用层分别二次
  校验读取/运维 Scope，并验证可信租户、严格参数、固定最小投影、终态记录和
  复位后状态；`provider_recovered`、`approved_exception`、`credentials_fixed`
  与 `route_fixed` 只匹配各自失败类别。成功、失败及人工复位在禁用自动时间戳
  时显式维护 `updatedAt`。33 项专项测试达到
  100%/98.07%/100%/100%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm precheck` 与 `pnpm check`；标准 MCP 仍不暴露异常队列或 R2 重试。
- OP 审批结果回传已覆盖 71 项 Outbox 身份绑定、终态筛选、桥接版本单调性、
  投递幂等冲突、事务与租约竞争、HMAC 最小载荷、响应回显、退避/死信及审计隔离
  测试；Relay 必须精确绑定信封与 Outbox 元数据，桥表通过精确旧版本乐观锁推进，
  外呼成功后的本地终态故障不得被通用失败处理回写或触发再次外呼，只记录
  `OP_APPROVAL_RESULT_LOCAL_FINALIZE_FAILED`；外呼终态后的审计故障只形成稳定
  告警，外部自由文本错误不得落库。两个目标文件
  合计覆盖率达到四维 100%，独立 90% 门禁已接入 `pnpm check`。
- 2026-07-29 已加固审批 Outbox 运行时边界：十七类模板、历史、实例与委托事件
  全部采用无未知字段的独立 payload 白名单，并校验可信租户、规范 UTC 时间、
  正安全版本、摘要、迁移状态/动作数、决策结果/终态/代理关系、转交差异、
  撤回唯一收件人与三十天委托闭包。payload 先规范复制，再由权威身份字段覆盖，
  禁止伪造租户、聚合、版本或夹带表单正文；受损事件在 Outbox 写入及 OP 结果桥
  消费前失败关闭。51 项专项测试达到四维 100%，独立逐文件 90% 门禁已接入
  `pnpm precheck` 与 `pnpm check`；标准 MCP 契约与风险分级不变。
- OP 经营摘要与审批请求 Webhook 双入口已覆盖 125 项控制器 query 禁止、
  六认证头、HMAC 原始字节、时间窗、可信 clientId 租户绑定、路由、防重放、
  Inbox 唯一键竞态、AES-256-GCM 密钥轮换/AAD/篡改、正文上限和审计故障测试；
  验证拒绝或成功入箱后的审计异常只形成稳定告警，不覆盖原始 HTTP 决策。
  两个控制器、两个入口服务和两个独立加密服务合计覆盖率达到
  98.34%/96.13%/100%/100%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- OP 经营摘要可靠处理链路已覆盖 114 项入箱排队、可信 Worker 上下文、任务与
  租户隔离、认领栅栏、租约恢复、4xx/5xx 分类、重试耗尽、事务 Outbox、最小
  REST/MCP 投影和提交后审计隔离测试。BullMQ JobId 必须绑定租户、Inbox 与
  载荷摘要；Worker 只可用当前任务标识和随机令牌推进状态，同任务可恢复未完成
  终态，过期任务须等待租约后接管。业务事务提交后若终态或审计写入异常，不得
  通用回写失败或再次执行外部副作用；OP 审批结果的 R2 决策也保持相同隔离。
  五个目标生产文件合计覆盖率达到 99.34%/97.35%/100%/100%
  （语句/分支/函数/行），逐文件四维 90% 门禁已接入 `pnpm check`。REST 与
  标准 MCP 均只返回日期、修订、币种和五项经营指标，不暴露内部标识、载荷摘要
  或接收时间，也不开放重试、对账和运维写能力。
- Treasury Outbox 已覆盖 13 项可信租户、精确事件字段、状态、强认证方法、
  文件摘要、回盘与对账最小载荷测试；覆盖率达到 98.48%/98.67%/100%/98.46%
  （语句/分支/函数/行），独立 90% 门禁已接入 `pnpm check`。
- Phase 4 考勤、薪酬和资金 REST 入口已覆盖 32 项固定路由、HTTP 方法、最小
  Scope、旧算薪边界守卫、幂等键、月份格式、强认证令牌、ETag、最小审计载荷
  和提交后审计隔离测试。业务事务、WORM 或银行副作用提交后的审计故障只记录
  不含异常正文和业务载荷的稳定告警，不向客户端反向暴露失败；敏感读取审计仍
  失败关闭。三个控制器合计四维覆盖率均为 100%，独立逐文件四维 90% 门禁已
  接入 `pnpm check`；MCP 风险边界未扩大。
- 审计链 WORM 锚定服务已覆盖 8 项验链、链头漂移、不可变回执幂等与竞争、
  批量边界和并发收敛测试，覆盖率达到四维 100%；独立 90% 门禁已接入
  `pnpm check`。
- 审计链验证服务已覆盖连续序号、前向哈希、事件 HMAC、链头一致性、空链、
  资源标识和 1,000 条满批分页，覆盖率达到四维 100%；独立 90% 门禁已接入
  `pnpm check`。
- 组织主数据应用服务已覆盖 34 项可信租户、组织视图裁剪、岗位/职级/员工引用、
  入职与迁移事实幂等、Care 离职终态、乐观锁和失败关闭测试；覆盖率达到
  97.44%/93.52%/100%/97.50%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- 组织主数据入口控制器已覆盖 31 项完整 REST、精确 Scope、ULID、幂等键、
  强 `If-Match`、ETag、Care 离职边界和审计终态测试；主数据写入后的审计故障
  只作独立告警，不反向暴露为业务失败。覆盖率达到四维 100%，独立 90% 门禁
  已接入 `pnpm check`。
- 薪酬主数据服务已覆盖 17 项可信连接器、审批/WORM 证据、L4 加密回读、
  生效区间与版本链、员工引用、规则确定性和并发失败关闭测试；覆盖率达到
  四维 100%，独立 90% 门禁已接入 `pnpm check`。
- 专业算薪主数据快照已覆盖 25 项固定路由、最小 Scope、可信服务/系统任务、
  脱敏投影、200 条分页、租户绑定摘要、规范 Base64URL、精确游标字段、合法
  页边界、跨租户/变更后重放拒绝和批量读取审计测试。审计只记录页计数与摘要，
  不记录人员正文；审计不可用时读取失败关闭。服务与控制器合计覆盖率达到
  98.21%/96.42%/100%/100%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`；该批量接口不注册为 MCP 能力。
- 薪酬四方对账服务已覆盖 18 项 Treasury/银行/税务证据、职责分离、差异冻结、
  迁移不可变回放、周期并发和状态证据一致性测试，并修复历史 `updatedAt`
  未持久化导致幂等回放失败的问题；覆盖率达到
  96.42%/96.79%/100%/98.96%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- 薪酬税务申报服务已覆盖 28 项 WORM 清单、组织身份、人员职责分离、WebAuthn、
  生产授权、税局回执、迁移重放、明文销毁、乐观锁和失败关闭测试；生产授权摘要
  已绑定不可变对象证据、审批人和强认证证据。覆盖率达到
  98.88%/98.58%/100%/100%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- Payroll Tax 双出口已覆盖 131 项固定标准 HTTPS、运行时凭据、规范清单
  UTF-8/JSON 根绑定、WORM 对象键/摘要/十年保留期、最小税务提交、短时生产
  授权、确定性幂等、非 2xx 正文隔离、Content-Length、16 KiB 流式硬上限、
  读取清理和完整回执 Schema 绑定测试。三个目标生产文件合计达到
  99.35%/98.57%/100%/99.25%（语句/分支/函数/行），且逐文件四维均不低于
  90%；独立门禁由个税申报总门禁接入 `pnpm check`。REST 与标准 MCP 继续只读
  脱敏控制摘要，不暴露税务正文、WORM 地址、凭据、归档或提交能力；真实税务
  沙箱、WORM 保留证明、限流、Secret 轮换和生产授权域仍待现场验收。
- 薪酬 L4 数据加密服务已覆盖 8 项上下文/AAD、密钥轮换、严格密钥环、
  IV/Tag 固定长度、规范 Base64URL、密文篡改、8 MiB 上限和失败关闭测试；
  外部密文在解码前执行长度约束，非规范主密钥稳定归类为密钥环错误。覆盖率达到
  96.51%/94.54%/100%/98.59%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- 资金支付服务已覆盖 22 项锁定工资、银行账户密文、WORM 文件、WebAuthn
  审批、生产授权、银行提交、迁移回放、明文销毁和并发失败关闭测试；生产授权
  摘要绑定不可变对象证据、批准人与强认证证据，覆盖率达到
  93.88%/93.23%/96.05%/95.77%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- Treasury WORM 证据出口已覆盖 98 项固定标准 HTTPS `POST /v1/objects`、
  运行时独立凭据、十年至一百年保留期、租户/批次 ULID、确定性幂等、pain.001
  固定 Schema、唯一 MsgId/PmtInfId、DOCTYPE/ENTITY 禁用、对象键与摘要绑定、
  非 2xx 正文隔离、严格 JSON Content-Type、Content-Length、16 KiB 流式硬
  上限、Fatal UTF-8、读取取消与完整回执反向绑定测试。目标适配器达到
  100%/97.77%/100%/100%（语句/分支/函数/行），独立逐文件四维 90% 门禁由
  资金支付总门禁接入 `pnpm check`。REST 与标准 MCP 不新增归档、文件读取或
  资金写能力；真实 Object Lock、法定保留证明、Secret 轮换、断连和限流仍待
  现场验收。
- Treasury 银行回盘服务已覆盖 37 项可信连接器、签名与恶意文件证据、密文金额、
  乱序/未来时间、迁移重放、支付指令冻结、乐观锁和失败关闭测试；覆盖率达到
  97.04%/96.27%/100%/97.05%（语句/分支/函数/行），独立 90% 门禁已接入
  `pnpm check`。
- Treasury 银行回盘 Inbox 入站边界已覆盖 65 项固定标准 HTTPS
  `POST /v1/returns/claim`、固定 Header、独立凭据、确定性幂等、非 2xx
  正文隔离、Content-Length、4 MiB 流式硬上限、读取取消、严格 UTF-8/JSON、
  完整清单 Schema 与领取对象精确绑定测试；验签或恶意文件负面证据保留给应用
  服务执行整批冻结。目标适配器达到 98.90%/96.15%/100%/98.70%
  （语句/分支/函数/行），独立逐文件四维 90% 门禁由回盘服务门禁接入
  `pnpm check`；真实银行回盘验签、恶意文件、限流与 WORM 联调仍待现场验收。
- Treasury L4 数据加密服务已覆盖 10 项上下文/AAD、独立加密与盲索引密钥环、
  无停机轮换、规范 Base64URL、固定 IV/Tag、外部密文预解码长度、8 MiB 上限和
  失败关闭测试；非规范主密钥稳定归类为密钥环错误，持久化 Schema 同步固定真实
  IV 编码长度。覆盖率达到 98.26%/96.82%/100%/99.00%（语句/分支/函数/行），
  独立 90% 门禁已接入 `pnpm check`。
- Phase 6 生产执行授权服务已覆盖 10 项发布物/主体精确绑定、HTTPS 端点、
  一次性 WORM 证据、响应体上限、严格 JSON、短时窗口与上游失败关闭测试，
  并将非法 URL 统一映射为稳定领域错误；覆盖率达到四维 100%，独立 90%
  门禁已接入 `pnpm check`。
- 2026-07-27 已补齐 Phase 4 Attendance 的 Employment 有效期、版本化日班次、
  跨日归属、Provider 提交水位/Inbox 对账与月结摘要哈希；真实 Provider 沙箱
  和生产 Replica Set 上的 v2 索引 apply 仍待现场证据。
- 2026-07-27 已补齐 Phase 4 Payroll 月中薪酬与跨法域自然日 HALF_UP 分摊：
  工资运行只接收精确档案引用，校验整月无缺口/无重叠，把档案版本、法域和分摊
  边界冻结进 L4 输入快照；不同考勤费率缺少逐日归属时失败关闭。
- 2026-07-27 已交付锁定工资补发/冲销的确定性差额准备、专用 Approval 审批和
  独立 WebAuthn UV 锁定：客户端不提交金额，重算、送审、审批与锁定职责分离，
  事件不含人员或金额。正向调整现可从原代发批次创建唯一单行 Treasury 补发子批次，
  并在直接或恢复全额成功回盘时事务性回写现金结算；负向调整建立唯一员工应收，
  支持追加式银行回款或带法定授权证据的工资抵扣；税务更正采用独立密文、WORM、
  WebAuthn 审批和税局提交，现金与税务均终结后才进入 settled。
- 2026-07-30 已补齐员工年度税局评估可信接入：公开制备入口不再接受客户端
  `officialAssessment`，受信任服务改由隔离税务网关读取并验证绑定租户、员工、
  税年和请求摘要的 Ed25519 回执；仅 `requires_employee_settlement` 状态的
  员工本人可创建五分钟内失效、严格官方同源的办理链接。审计不记录一次性 URL，
  幂等账本只保存记录 ID/版本/摘要并从密文重放，标准 MCP 未新增评估、链接、
  申报或收付能力。税务 HTTP 151 项和年度/调整
  306 项专项测试通过，新适配器覆盖率为 93.75%/92.59%/100%/96.22%。
  真实税局沙箱、密钥轮换、断连恢复、现场金样例与薪税签署仍未完成。
- 2026-07-30 在提交 `cdcef767fbbbd5c9dc5111456c1acdfa55c48bf9` 上重新执行
  Kimi Code CLI 0.28.1 正式 ACP 探针和 MCP Inspector CLI 2.0.0 正式目录
  探针。Kimi 连接 stdio 并发现 50 个 Tool；Inspector 逐项发现 50 个 Tool、
  4 个静态 Resource、27 个 Resource Template 和 25 个 Prompt，二者共同绑定
  `catalogHash=sha256:22316539594c425ab2a3547b0d5899196d8cbd11a7bf205e56250a602e1bbd91`，
  Inspector 另绑定
  `runtimeContractHash=sha256:61c70d725186519dd7473857f69cd2e85e4846232930f60aab2e608e166b14a9`。
  两个探针均未调用模型或业务 Tool；Claude/Cursor 未安装，不能据此宣称实体
  客户端通过。
- Draft PR #129 的早期 Hosted Actions run `30493181184`、`30493181191`、
  `30493181203` 和 `30493181220` 曾在步骤执行前受账户状态拦截，不能记为代码
  失败或门禁通过。仓库公开后已在后续候选修复 CI，并通过堆叠 PR 合入
  `main@6219644`；2026-08-01 的 main run `30685077655`、`30685077683` 和
  `30685077646` 分别实际通过 Phase 1、Phase 5 和文档门禁。CI 已解除阻断，
  但真实外部联调、迁移演练、UAT、切换和 Hypercare 仍保持未完成。

## 目录约定

- `apps/erp-api/`：NestJS API、领域模块、集成适配器、Worker 与迁移代码。
- `apps/erp-web/`：Next.js 管理工作台、移动工作台、OAuth 与 MCP 确认页面。
- `apps/website/`：Next.js 中英文官网、SEO、CMS 已发布内容读取与预约转化页面。
- `packages/`：共享类型与安全工具。
- `deploy/`：生产 Helm Chart、Kubernetes 平台护栏与可观测性基线。
- `scripts/`：安全、迁移、MCP、容量、韧性、发布和现场证据验证器。
- `docs/phase-0/`：企业架构、领域数据、集成、MCP、安全质量与 GitHub 治理规范。
- `docs/marketing-cms.md`：双语官网、CMS、媒体、AI、线索与现场配置交付边界。
- `wordpress backup/`：历史备份，明确排除，不得纳入 Git、扫描或应用构建。

## 代码与安全规范

- 回复、代码注释、文档注释、迁移说明和 Git commit message 必须使用中文。
- TypeScript 使用 ESM 与 async/await，禁止 `var`。
- 禁止硬编码密码、密钥、Token、银行账号、电子签密钥和生产环境标识。
- 禁止信任客户端租户标识；动态字段、排序和查询必须经过白名单映射。
- MCP 禁止直接访问数据库、透传上游 Token 或注册 R3 操作。

## 验证命令

```bash
pnpm install --frozen-lockfile
pnpm audit
pnpm check
NEXT_PUBLIC_WEBSITE_ORIGIN=https://www.example.invalid \
NEXT_PUBLIC_ERP_API_ORIGIN=https://erp.example.invalid \
NEXT_PUBLIC_MARKETING_CAPTCHA_WIDGET_ORIGIN=https://captcha.example.invalid \
NEXT_PUBLIC_MARKETING_CAPTCHA_WIDGET_URL=https://captcha.example.invalid/widget \
pnpm build
```

`pnpm check` 覆盖 Lint、TypeCheck、单元/集成/协议测试、文档、安全、镜像、
Kubernetes、MCP、迁移、容量、韧性、发布和 Phase 6 证据门禁自测。本地自测
不能替代目标环境中的真实联调、生产等价演练或人工签署。两个 Web 应用的生产
构建必须显式提供公开 HTTPS Origin；`.invalid` 仅用于不发布产物的工程构建门禁。

需要本地基础设施的真实 MCP 握手使用：

```bash
pnpm --filter @gaoq/erp-api smoke:mcp:live
pnpm --filter @gaoq/erp-api smoke:worker:live
```

脚本仅在内存生成临时密钥，使用独立测试数据库与 Redis DB，结束时关闭其创建的
API 或 Worker 子进程。

`Phase 1 工程质量门禁` 已在同一个 GitHub-hosted Runner 的构建步骤后自动启动
临时 MongoDB Replica Set 与 Redis，并执行上述两项真实运行冒烟；MCP 目录直接与
受控目录逐项比对，避免数量和名称随业务演进发生静默漂移。该证据仅代表当前提交
的 GitHub 临时运行时集成通过，不替代外部供应商沙箱、生产等价、UAT、切换与
Hypercare 现场验收。

`Phase 5 安全与供应链门禁`进一步创建固定摘要的 Kind/Kubernetes 1.30.8 集群，
把四类生产镜像以不可变 digest 交给生产 Helm Chart 部署，并验证全部 11 个
应用 Pod、ClusterIP 健康、Worker 指标鉴权、OAuth 与官方 MCP SDK 完整目录。
临时凭据与测试依赖在 Runner 内生成并清理。该门禁补齐真实 Kubernetes API 和
多副本服务运行证据，但不替代目标云的多可用区、准入/网络策略、托管数据、
KMS/WORM、监控、外部连接及现场签署。

## Codex 执行规范

- Codex 执行入口为 `.codex/AGENTS.md`，完整项目规范以 `AGENTS.md` 为准。
- Codex 默认直接执行，不要求调用指定外部代理；只读任务禁止写计划文件。
- 大型盘点必须拆成小范围任务，并输出文件路径、调用方向和实现建议。
- 所有实现结果必须经过复核和验证，未经检查不得合并。
