import { pathToFileURL } from 'node:url'; import { createConnection } from 'mongoose';
import { SourcingRequestSchema } from '../modules/sourcing/persistence/sourcing.schema.js'; import { buildIndexManifestFromSchemas, runAdditiveIndexMigration } from './phase-3-indexes.js';
const MIGRATION_ID = 'phase-s2-sourcing-indexes-v1';
export function buildPhaseS2SourcingIndexManifest() { return buildIndexManifestFromSchemas(Object.freeze([SourcingRequestSchema])); }
export async function runPhaseS2SourcingIndexMigration(connection: Parameters<typeof runAdditiveIndexMigration>[0], dryRun: boolean) { return runAdditiveIndexMigration(connection, dryRun, { migrationId: MIGRATION_ID, manifest: buildPhaseS2SourcingIndexManifest() }); }
async function main(): Promise<void> { const args = process.argv.slice(2).filter((argument) => argument !== '--'); if (args.some((argument) => argument !== '--dry-run')) throw new Error('PHASE_S2_SOURCING_INDEX_ARGUMENT_INVALID'); const uri = process.env.MONGODB_URI; if (uri === undefined || !uri.startsWith('mongodb://')) throw new Error('PHASE_S2_SOURCING_INDEX_MONGODB_URI_REQUIRED'); const connection = createConnection(uri, { autoIndex: false, serverSelectionTimeoutMS: 5_000 }); try { await connection.asPromise(); const result = await runPhaseS2SourcingIndexMigration(connection, args.includes('--dry-run')); process.stdout.write(`${JSON.stringify({ migrationId: MIGRATION_ID, mode: args.includes('--dry-run') ? 'dry-run' : 'apply', ...result })}\n`); } finally { await connection.close(); } }
const entryPath = process.argv[1]; if (entryPath !== undefined && import.meta.url === pathToFileURL(entryPath).href) void main().catch((caught: unknown) => { const code = caught instanceof Error && /^PHASE_S2_SOURCING_INDEX_[A-Z_]{1,96}$/u.test(caught.message) ? caught.message : 'PHASE_S2_SOURCING_INDEX_DATABASE_FAILURE'; process.stderr.write(`${code}\n`); process.exitCode = 1; });
