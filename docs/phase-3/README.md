# Phase 3：人才与学习闭环

Phase 3 以候选人到正式员工的完整证据链为主线，交付招聘、e签宝、入职、知识培训、关怀及对应 MCP 能力。实现顺序为：领域与数据契约 → 招聘主流程 → 电子签 → 入职与组织转化 → 培训考试 → 关怀与离职 → MCP 与生产验收。

当前仓库已交付候选人、申请、HC、职位、面试/日历、Offer
审批/投递/接受证据、Recruitment MCP、招聘门户、智能简历库、eSign 验签入箱，
以及 Onboarding 证据任务与建档 Saga。组织域已分离
`Person`、`Employee`、`Employment`，Onboarding MCP 只提供按部门裁剪的 R0
摘要，R3 建档不向 AI 开放。

招聘渠道已交付 Adapter/Normalizer/EvidenceVerifier、加密 Inbox、盲指纹去重、
加密游标、可靠补拉/重放及状态回传；智能简历库已交付隔离网关契约、OpenAI
严格结构化输出、受控词表、异步分析、人工确认和管理页面。来源连接强制 HTTPS、
租户回显、有界 JSON 与 NFKC 后直接标识检查；模型连接强制 `store:false`、哈希
`safety_identifier`、拒答/未完成失败关闭，队列使用租户绑定确定性 JobId 与版本
租约。招聘渠道补拉进一步闭合 Mongo 回读绑定、精确批量/投递 Envelope、规范
事件 ID/UTC 时间、批内唯一事件和前进游标；原始 JSON 在任一 Inbox 写入前执行
512 KiB 单条、4 MiB 整批及深度/节点/数组/对象键预算，规范深冻结后才加密。
85 项专项测试使 Registry 与补拉服务合计达到
98.22%/95.47%/100%/98.40%（语句/分支/函数/行），两个文件逐文件四维均不低于
90%；标准 MCP 不开放渠道补拉、游标、Inbox、凭据、证据处置或重放。真实招聘
渠道、简历扫描/对象存储/脱敏网关、OpenAI Secret、API Project
数据控制及招聘 UAT 仍待现场验收。

招聘渠道出站副作用已进一步加固：职位发布/下架和申请阶段回传只认领
`pending`，过期 `processing`、未分类传输异常及平台响应后的本地终态故障统一
隔离为 `manual_review`；只有 Adapter 明确声明 `not_committed` 才允许自动重试。
领取记录在建立可信租户上下文前校验，绑定、应用/职位投影、部门、加密映射和
精确回执均做运行时闭包。受控人工处置 REST 强制可信租户、幂等键、R2 审计，
结果未知任务必须同时具备 `approved_exception` 和供应商确认未提交，且不能
直接伪造成功。职位、阶段和处置三个专项门禁分别覆盖 40、30、19 项测试，达到
95.45%/92.38%/100%/97.25%、96.52%/92.36%/100%/97.72% 与
100%/94.11%/100%/100%（语句/分支/函数/行）；标准 MCP 不开放人工核验或
重试。真实渠道沙箱回执语义和人工处置 UAT 仍待现场验收。

招聘面试日历可靠投递已加固：Outbox 扇出目标运行时复核并去重，投递 Worker
只认领 `pending`，过期 `processing` 隔离为结果不确定；不可变事件版本与当前
投影严格核对，飞书多步写入保留部分提交事件标识，平台成功后的本地终态或审计
故障不再进入通用失败回写。人工处置 REST 强制可信租户、幂等键、R2 审计和
`approved_exception`，MCP 继续只读且不暴露日历写入、重试、外部标识或凭据。
人工处置正文严格拒绝未知字段，分页数字只接受规范十进制；业务失败审计故障保留
原始异常，提交后成功审计故障不改变处置结果。169 项专项测试覆盖九个生产文件，
合计达到 99.16%/97.79%/100%/99.76%（语句/分支/函数/行），逐文件四维均不低于 90%；
真实钉钉/飞书日历沙箱、限流、权限与人工核验流程仍待现场验收。

Offer REST、DTO 与领域入口已进一步加固：资源 ULID、强 ETag、8–128 位白名单
幂等键和精确正文在副作用前失败关闭，无正文动作只允许缺失正文或严格空普通对象。L4 条款
同时执行 DTO 与领域精确白名单、规范毫秒级 UTC、真实日历日期、安全版本和单调
时间校验；业务失败审计故障保留原始异常，提交后的成功审计故障只作低敏告警。
165 项入口专项测试使 Controller、DTO 与领域文件四维均为 100%；27 项应用服务
测试达到 95.97%/94.05%/100%/96.84%，两道逐文件四维 90% 门禁已接入
`pnpm precheck`、`pnpm check` 和全局覆盖链。REST、事件和标准 MCP 契约不变，
MCP 仍只复用脱敏应用服务，不开放审批、投递、候选人决定或签署写能力；真实
Approval、eSign、招聘渠道及 HR UAT 仍待现场验收。

eSign 单调状态投影已进一步加固：当前状态、供应商状态和人工复核组合先通过严格
运行时校验；未知状态和终态冲突保留可信终态，不自动前进或倒退；重复异常事件按
完整下一投影返回 `changed=false`，不会重复增加版本。本地 `completed` 收到正常
完成状态时保留既有复核原因。20 项直接测试使投影文件四维均为 100%，并纳入
现有电子签十文件逐文件 90% 门禁；标准 MCP 不新增 eSign 写能力。

eSign 发起链路已补齐持久化意图、可信 Worker、租约恢复和人工核验状态机。
供应商调用前只保存加密文件标识与 Offer 控制事实；外部结果未知时进入
`manual_review` 且禁止自动重放，已取得 flowId 时只补本地登记。发起、终态查询
和人工处置 REST 分别使用专用 Scope、幂等键、可信租户和 R2 审计；人工重试要求
批准例外及供应商确认未创建，人工绑定不能直接伪造成功。139 项专项测试达到
97.95%/94.98%/100%/98.74%（语句/分支/函数/行），五个目标生产文件逐文件
四维均不低于 90%；标准 MCP 永久不开放发起、重试、人工处置、外部标识或签署
主体。真实 eSign 租户、实名认证、人工核验流程和业务 UAT 仍待现场验收。

Knowledge 已交付版本发布、绝对进度、评分摘要、必修证明、可恢复 Onboarding
回填、严格 HTTPS 证据网关和只读 MCP。Care 已交付离职审批、四类清算证据、
时区绑定、Employment/Employee/Identity 原子终止、校友授权自动到期，以及
生日/周年关怀可靠编排。校友授权终止后的下游清理证明也已交付撤回/到期统一
编排、独立目标信任域、Ed25519 不可变证明、对账/重放/重建、追加迁移、指标、
REST 和只读 MCP；运行规范见
[校友授权终止后的下游清理证明运行手册](./05-alumni-cleanup-proof-runbook.md)。
清理证明出口进一步固定标准 HTTPS 方法、路径和协议 Header，运行时重算任务
ID/控制摘要并复核执行租约与目标政策；目标 Origin、凭据、Ed25519 Key ID/公钥
均禁止跨目标复用。成功回执须通过原始字节验签、16 KiB 流式限长、Fatal UTF-8、
完整 Schema、请求上下文、存储引用和最低保留期绑定。131 项专项测试达到
96.39%/96.15%/96.42%/97.59%，三个生产文件逐文件四维均不低于 90%。

生日/周年关怀通知网关已进一步加固：严格请求和唯一偏好渠道在外呼前拒绝未知
字段、联系方式、正文或 Token；运行时复核独立 HTTPS 端点、可见 ASCII 凭据、
规范 Key ID 与 Ed25519 公钥。回执使用严格 JSON Content-Type、Content-Length、
16 KiB 有界流、异常取消、Fatal UTF-8、规范 Base64 和原始字节验签，并闭合
租户、任务、控制摘要、渠道及送达时间。25 项专项测试使目标 Adapter 达到
95.59%/94.44%/92.85%/96.66%（语句/分支/函数/行），逐文件四维 90% 门禁已
接入 `pnpm precheck`；标准 MCP 仍只读本人脱敏汇总，不新增发送或证据能力。

生日证明 L4 写入口已进一步加固：REST 仅接受严格 ULID、强 `If-Match`、白名单
幂等键与规范日历月日，应用服务再次复核可信服务主体、Scope、版本、请求结构及
幂等结果的对象/版本闭包。业务失败审计故障保留原始异常，提交后的成功审计故障
只形成低敏告警，不诱发身份服务重做；响应、审计和日志均不含月日或证据标识。
73 项专项测试使 DTO、应用服务与控制器达到 100%/100%/100%/100%，独立逐文件
四维 90% 门禁已接入 `pnpm precheck` 和 `pnpm check`。标准 MCP 不新增生日登记、
盲索引解析或证明读取能力。

Care 事务 Outbox 发布边界已进一步加固：离职案件、校友授权、授权终止清理及
生日/周年关怀共 22 类事件采用完整对象和逐类型负载白名单，严格绑定状态、版本、
规范时间、可信租户/主体和活动 Mongo 事务；个人、联系方式、授权证据、清理证明、
通知正文、Token 及任意未知字段均在落库前失败关闭。创建回执还须反向绑定事件
ULID、聚合、完整 CloudEvent 和调度时间，避免受损数据库结果被误报为发布成功。
60 项专项测试使目标 Writer 达到 98.71%/91.89%/100%/100%
（语句/分支/函数/行），独立四维 90% 门禁已接入 `pnpm precheck` 与
`pnpm check`；事件名、REST 和标准 MCP 契约不变，MCP 仍不开放发送、重放、
证明或联系方式能力。

Onboarding 事务 Outbox 发布边界已进一步加固：创建、任务完成、建档开始和完成
四类事件采用严格对象与逐类型负载白名单，状态、任务代码、Employment 引用、
规范时间、可信租户/主体和活动 Mongo 事务必须完整闭合；未知字段、敏感证据、
Token、跨租户事件及分裂身份上下文均在写入前失败关闭。数据库创建结果还须反向
绑定事件 ULID、聚合版本、完整 CloudEvent 和调度时间。43 项专项测试使目标
Writer 达到 98.27%/96.87%/100%/100%（语句/分支/函数/行），逐文件四维
90% 门禁已接入 `pnpm precheck` 与 `pnpm check`。REST、事件名和标准 MCP
契约不变；MCP 仍经入职应用服务只读部门裁剪摘要，不开放任务、建档、Outbox、
重放或证据读取能力。真实招聘/eSign/Org 端到端回放与 HR UAT 仍待现场验收。

Onboarding 聚合与任务证明仓储边界已进一步加固：所有读取使用固定最小投影，
逐条反向绑定可信租户、实例/Offer/候选人查询主键、完整任务证明、状态、版本及
规范时间；候选人时间线强制一百条硬上限、稳定顺序和唯一标识。聚合插入、乐观锁
更新和证明追加均要求活动 Mongo 事务，创建与更新回执还须绑定全部不可变引用和
目标终态。104 项专项测试使目标仓储文件语句、分支、函数和行覆盖率均为 100%，
独立逐文件四维 90% 门禁已接入 `pnpm precheck` 与 `pnpm check`。REST、事件
与标准 MCP 契约不变；MCP 仍不读取证明、直连仓储或执行 R3 建档。

Recruitment 事务 Outbox 发布边界已进一步加固：申请、候选人、HC、职位、面试、
Offer 与简历分析 31 类事件采用严格对象与逐类型负载白名单，聚合、状态、审批/
投递/接受/eSign 证据引用、规范时间、可信租户/主体及活动 Mongo 事务必须完整
闭合；未知字段、L3/L4 原文、Token、跨租户事件和分裂身份上下文在写入前失败
关闭。数据库创建结果须反向绑定事件 ULID、聚合版本、完整 CloudEvent 与调度
时间。83 项专项测试使目标 Writer 达到 99.29%/98.43%/100%/100%
（语句/分支/函数/行），逐文件四维 90% 门禁已接入 `pnpm precheck` 与
`pnpm check`。REST、事件名和标准 MCP 契约不变；MCP 不直连 Outbox，也不
开放发送、审批、重放或证据读取。真实招聘渠道、日历、eSign、事件总线和招聘
UAT 仍待现场验收。

Talent Lifecycle REST 写入口与事务 Outbox 发布边界已进一步加固：入口对资源
ULID、白名单幂等键、强 `If-Match`、严格请求结构和未知字段失败关闭；业务失败
记录独立 R2 失败审计，事务提交后的成功审计故障只告警，不把已提交终态回写为
失败。创建、完成和取消三类触点事件严格绑定可信租户、动作/状态/版本、活动
Mongo 事务、最小 CloudEvent 负载和数据库回执，备注、负责人、方向、姓名与
联系方式均不进入事件。80 项专项测试使入口控制器和 Writer 四维均达到 100%，
两个独立四维 90% 门禁已接入 `pnpm precheck` 与 `pnpm check`。REST、事件名和
标准 MCP 契约不变；MCP 继续只读，不开放触点写入、Outbox 或重放。真实事件
总线、角色映射和跨角色 UAT 仍待现场验收。

Talent Lifecycle 360 已交付 `Candidate → Person → Employment` 跨域投影、
生命周期推导、加密服务触点、授权门禁、Outbox、只读 MCP 和管理页面。eSign V3
Adapter 已覆盖流程补拉、已签 PDF 短时下载、供应商验签、证据账本及 Offer
`signed` 门禁；病毒扫描与 WORM 归档缺少成套配置时失败关闭。

真实 e签宝、招聘渠道、通知、日历、简历基础设施、Knowledge 证据网关、Care
清算/授权校验器、CRM/校友平台清理证明、生产索引、角色映射、历史回放及各业务
UAT 尚未形成现场证据，因此 Phase 3 只能标记“代码已交付”，不得标记生产完成。

Knowledge 授权全文检索代码已交付：可信任职及部门/岗位裁剪、应用服务二次授权、独立签名搜索网关、课程发布/下架事务索引任务、重试/死信、低基数新鲜度指标、追加迁移、只读对账、全量重建以及标准 MCP Tool/Resource/Prompt。真实搜索集群的中文/英文分词、撤权收敛、性能、安全与业务 UAT 仍待现场验收；代码交付不得描述为生产完成。

Knowledge 搜索索引事务任务写入边界已进一步加固：只接受规范事件 ULID、可信
租户和最小课程投影，固定“已发布课程只能 `upsert`、已下架课程只能 `delete`”；
部门/岗位授权集合必须安全、唯一、稳定排序且总量有界。写入要求活动 Mongo
事务，并反向绑定数据库创建回执中的课程版本、操作、授权投影、调度时间及完整
初始状态，空、多条、错租户、错版本或被篡改回执均使事务失败。47 项专项测试使
目标 Writer 达到 100%/98.24%/100%/100%（语句/分支/函数/行），逐文件四维
90% 门禁经 Knowledge 持久化组合门禁接入 `pnpm precheck` 与 `pnpm check`。
REST、事件和标准 MCP 契约不变；MCP 仍复用应用服务，不能直连任务集合或搜索
网关，也不开放索引写入、重放或凭据。

Knowledge 可靠考试编排代码已交付：版本化题型/时限/次数/评分与通过策略、超时自动提交、人工复核、Mongo 权威状态机、Worker 退避/死信/熔断、事务 Outbox、低基数 SLA 指标、追加索引、只读对账、显式重放和本人只读 MCP。运行规范见 [考试编排与评分运行手册](./03-knowledge-exam-orchestration-runbook.md)。真实评分沙箱、代表题集和业务 UAT 仍待现场验收。

Knowledge 考试持久化与事务事件边界已进一步加固：运行仓储只接受规范标识和可信
租户，使用最小投影并反向绑定考试、任务、状态、版本、题型策略、网关/提交/复核
证据及完整时间线；尝试号分配、插入和提交 CAS 均要求活动 Mongo 事务，创建与
更新回执须绑定预期终态。Knowledge Outbox 对课程、任务、考试运行、评分与入职
证明共 15 类事件执行逐类型严格负载白名单，拒绝答案、标准答案、Token、未知字段
及状态/题型错配。124 项专项测试使两个生产文件达到
95.71%/94.06%/100%/97.42%（语句/分支/函数/行），且逐文件四维均不低于
90%；门禁已接入 `pnpm precheck` 与 `pnpm check`。REST、事件名和标准 MCP
契约不变，MCP 仍只读本人脱敏状态且不直连考试仓储或 Outbox。

Knowledge 评分证据与搜索网关信任边界已进一步加固：所有考试外呼先经严格请求
Schema，答案、Token、未知字段和题型/人工复核策略错位在网络调用前失败关闭；
人工复核状态与最终评分必须回显同一 `reviewEvidenceId`。评分与搜索 Adapter
运行时复核独立 Origin、凭据、Ed25519 公钥和 Key ID，执行请求体上限、严格
JSON Content-Type、Content-Length、流式 16 KiB 上限、Fatal UTF-8、规范
Base64 和异常正文取消。20 项专项测试使目标生产文件达到
92.17%/91.90%/98.00%/93.33%（语句/分支/函数/行），逐文件四维 90% 门禁
已接入 `pnpm precheck`；REST、事件和标准 MCP 契约不新增写能力。

## 强制边界

- ERP Recruitment 是候选人、申请和 Offer 的权威源；渠道只提供原始投递及参考状态。
- `Person`、`Candidate`、`CandidateApplication`、`UserAccount`、`Employment` 分离；同一候选人可以跨职位、跨招聘周期产生多个申请。
- Onboarding 只保存招聘、签署、材料和培训引用，不直接修改 Org 集合；完成门禁后由 Org 应用接口建立 Employment 并发布主数据事件。
- e签宝、招聘渠道、短信、邮件、日历和对象存储均通过 Integration 防腐层；供应商 Token、状态和对象键不得进入领域 API。
- Web、REST、MCP、Webhook 和 Worker 复用应用服务；R1/R2 写操作沿用服务端 `prepare → confirm → execute`，R3 不注册工具。
- 候选人 L3 与合同/身份/银行卡 L4 数据使用独立密钥域、盲索引、目的限制、访问审计和到期清理。

详细契约见 [领域与集成契约](./00-domain-integration-contract.md)。
